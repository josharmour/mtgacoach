# PROGRESS: review-distributed-runner (adversarial review of distributed-runner)

Date: 2026-07-31. Swarm task key: `review-distributed-runner`.
Reviewed: mtgacoach PR #472 + magezero local branch `distributed-runner`
(commit c65dbdc). Review performed from a fresh detached worktree
`/home/joshu/repos/magezero-wt-review` at c65dbdc (dev worktree not reused;
removed after review). Findings posted:
https://github.com/josharmour/mtgacoach/pull/472#issuecomment-5145800842

## Verdict: MERGEABLE

Every self-reported claim re-verified or independently reproduced.

## Measured re-verification

- Unit tests, fresh worktree: `tests/test_distributed.py` -> 12 passed in 0.51s.
- Backward compat, functional: sequential `.mz_tmp/game.yml` generated by the
  pre-branch runner (live working tree) and the branch runner on identical
  inputs against the real live `configs/game.yml`: byte-identical. Diff of
  live runner.py vs branch runner.py contains only the opt-in distributed
  additions; the sequential loop body is verbatim.
- `build_arm_game_cfg` (default local HostSpec) == sequential builder dict on
  the real live game.yml (threads=6, host=localhost in live config).
- run.json contract: historical run `runs/2026-07-29_03-19-18/run.json` gen
  keys = {completed_at, opponent_sessions, primary_sessions, settings},
  settings keys = {prior_temperature, priors, td_discount} — identical to
  `record_gen(arms=None)` output and to the shape locked by
  `test_record_gen_contract`.
- Integration smoke re-run (fresh process, gate load 14.9 < 17):
  INTEGRATION PASSED. Wall 152s vs 266s sequential-sum (1.75x); overlap
  proven by timestamps (starts 10:35:38.378/.379; ends 10:37:31 / 10:38:10);
  max(113.4, 152.4) = 152.4, sum = 265.8. Consistent with the dev's
  196s/316s (1.61x).
- Output separation: 4 distinct sha256 checksums; primaries both 5,107,696
  bytes (same preallocated size, different content). No shared magezero.log
  written; only the two slug-suffixed per-arm JVM logs (709,940 and
  1,132,959 bytes). WinRates.txt: exactly one correct line per arm.
- Server during concurrency: smoke used the new CUDA net server :50060
  (pid 476820, GPU 1, 4,832 MiB — under cap, coexisting with Qwen at
  56,078 MiB); its log had 0 error/exception/refused lines across ~14.9k
  requests. Live :50052 took no extra load in this re-run.
- Live workloads: curriculum JVM 276069 alive before/after (etime 2:56:48
  after), :50052 healthz ok, Qwen on :8002 answering. Smoke tmp dir
  `.mz_tmp/hosts/it-20260731_103538` (incl. 269MB db copy) removed.

## Claims verified by inspection

- Live :50052 loopback-only (ss: LISTEN 127.0.0.1:50052); `ensure_servers`
  raises for unreachable non-loopback targets.
- `xmage/log4j.properties` hardcodes `File=magezero.log` in three appenders,
  no `${log.file}` interpolation — `-Dlog.file` inert as claimed.
- `NotImplementedError` for checkpointed-mcts opponents present + unit-tested.
- c65dbdc folds in the live-ops working-tree diffs (MZ_SERVER_PYTHON /
  MZ_SERVER_ENV, MZ_SERVER_VRAM_GB cap, Linux launch_jvm); branch vs live
  working tree differs only by the distributed feature.
- No symlinks in generated scripts; prep warns when source H2 lock exists.
- PR #472 PROGRESS file: no emojis; branch commit has no Co-Authored-By.

## Findings (non-blocking)

1. Duplicate opponent deck in one generation: file-level isolation holds
   (constructed 2 same-deck arms — distinct slugs/dirs/logs/outputs), but
   both arms' wr_lines records contain both WinRates lines and the fallback
   attribution is order-dependent. The "unique within a generation"
   assumption is documented but unenforced.
2. Arm dirs keyed by gen/index/deck are reused across runs of the same
   curriculum; arm.log opens append, so a rerun whose JVM exits 0 without
   printing the win-rate line could parse a stale line. Low severity.
3. check_remote_host.sh's NFS round-trip check is cwd-relative (documented);
   1s attribute-cache sleep may be optimistic on CIFS.
4. Original smoke artifacts were deleted (unauditable); mitigated by the
   independent re-run above reproducing the result.
