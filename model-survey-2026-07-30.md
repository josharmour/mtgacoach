# Open-weight model survey — 2026-07-30

Target: replace DeepSeek V4 Flash on 2x RTX PRO 6000 Blackwell (sm_120),
leaving >=30 GB free for MageZero training + gemma-4-12B QLoRA.
Produced by a 15-agent survey/verify/recommend workflow (run wf_6048902b-813).

---

{
  "summary": "Survey current open-weight LLMs that fit 2x RTX PRO 6000 while leaving room for training",
  "agentCount": 15,
  "logs": [
    "survey returned 31 raw candidates across 4 angles",
    "verifying 10 unique shortlisted candidates",
    "verified 10; tiers: Nemotron 4 — CONFIRMED NON-EXISTENT as a 2026 model (name also collides with a stale 2024 release)=DOES_NOT_FIT NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4=TIER_A Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4=TIER_A NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4=TIER_A Qwen3.6-27B (Alibaba/Qwen)=TIER_A Qwen3.6-35B-A3B (Alibaba/Qwen)=TIER_A Gemma 4 31B (dense) and Gemma 4 26B-A4B (MoE), Google DeepMind=TIER_A DiffusionGemma 26B-A4B-it (Google DeepMind)=TIER_A NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B (deploy the -FP8 build, NOT -NVFP4)=TIER_A Mistral Small 4 (Mistral-Small-4-119B-2603)=TIER_B"
  ],
  "result": {
    "report": "# Replacing DeepSeek V4 Flash on 2× RTX PRO 6000 Blackwell Max-Q (sm_120)\n**Date: 2026-07-30 · Constraint: ≥30 GB free after weights+KV, service up, ~0.9–1.1 s per advice call**\n\n---\n\n## 1. Is there a Nemotron 4? No.\n\n**There is no Nemotron 4 as of 2026-07-30.** NVIDIA announced the \"Nemotron Coalition\" on **2026-03-16** at GTC, and the press release contains exactly one sentence naming it: the coalition's first base model *\"will underpin the upcoming NVIDIA Nemotron 4 family of open models.\"* No sizes, no architecture, no parameter counts, no timeline, no weights. Source: [nvidianews.nvidia.com — \"NVIDIA Launches Nemotron Coalition…\", 2026-03-16](https://nvidianews.nvidia.com/news/nvidia-launches-nemotron-coalition-of-leading-global-ai-labs-to-advance-open-frontier-models). The most recent Nemotron press release (2026-07-15) is a customer-adoption story announcing no base model. A HuggingFace search for 2026-created `nemotron-4` repos returns zero. **Do not plan around it.**\n\nTwo traps attached to that name:\n\n- **`nvidia/Nemotron-4-340B-Base/-Instruct` are real weights** — dated **2024-06-12** (arXiv 2406.11704). That is ~25 months stale and 340 B dense: ~340 GB at FP8, ~180–190 GB at NVFP4 against 191.2 GiB total. Fails on weights alone. This is the exact Llama-4-Scout failure mode — a real artifact with a recent-sounding name.\n- **AI-generated slop pages fabricate a roadmap.** Blogs claiming \"30B and 70B expected mid-to-late 2026\" and a model called `Nemotron-7B-Collab` were checked; that model does not exist on HuggingFace and NVIDIA published no sizes. Do not relay those as fact.\n\nThe newest *real* Nemotron is the **Nemotron 3** family, which does contain viable candidates (ranked below).\n\n---\n\n## 2. Ranked viable candidates\n\nFree-VRAM figures are **across the pair**, after weights + KV at 128k + runtime overhead. AA = Artificial Analysis Intelligence Index v4.1 (DeepSeek V4 Flash = **40** at Max Effort, **37** at High Effort — see §4, this is *not* the mode you run).\n\n| # | Model / quant | Release (verified) | Params | Ctx | Weights + KV@128k | Tier | Free VRAM | Measured decode on RTX PRO 6000 | vLLM sm_120 | Licence | AA |\n|---|---|---|---|---|---|---|---|---|---|---|---|\n| 1 | **Qwen3.6-27B-FP8** (dense) | 2026-04-21 (HF createdAt + QwenLM GitHub) | 27.8 B dense | 262 k | 28.75 + 4.0 GiB | **A** | **~138 GiB** | 100–137 tok/s | ✅ Best. Dense ⇒ never touches broken FP4/FP8 MoE grouped-GEMM. Published dual-RTX-6000 config exists | Apache-2.0 | 37 |\n| 2 | **Qwen3.6-35B-A3B-FP8** (MoE) | 2026-04-15 | 35 B / 3 B active | 262 k | 34.3 + ~10 GiB | **A** | ~135 GiB | **170–208.6 tok/s** measured; **72 tok/s** in bad config (vLLM #44688, open) | ⚠️ Triton MoE fallback; no tuned E=256/N=256 config | Apache-2.0 | 32 |\n| 3 | **Gemma 4 26B-A4B** FP8 / NVFP4 | 2026-03-31 (ai.google.dev releases) | 26.5 B / ~4 B active | 262 k | 26.67 + 2.70 GiB | **A** | ~157 GiB | none published; ~4.6 GB read/token ⇒ arithmetic ceiling ~196 tok/s BF16 | ⚠️ **Requires vLLM ≥ 0.21.0** (KV allocator fix, #43308: 55k→525k tokens) | Apache-2.0 | 26 |\n| 4 | **Nemotron-Labs-3-Puzzle-75B-A9B-FP8** | 2026-07-06 (model card \"Release Date\") | 75 B / 9.3 B active | 256 k–1 M | **77.33** + 0.50 GiB | **A** (tight, util ≈0.87) | ~108 GiB | none on sm_120 | ✅ `NemotronHPuzzleForCausalLM` registered in vLLM main | OpenMDW-1.1 | n/a |\n| 5 | **MiniMax-M2.7-NVFP4** | 2026-04 — **day unverified**, see §4 | 230 B / 10 B active | 200 k | ~116–120 + 10–15 GB | **B** | ~56–61 GB | none; proxy 50.5 tok/s (Qwen3.5-397B NVFP4 Marlin, 4× card) | ⚠️ NVFP4 MoE broken on sm_120 → `VLLM_MOE_FORCE_MARLIN=1` | custom (\"other\") | **38** |\n| 6 | **Nemotron-3-Super-120B-A12B-NVFP4** | 2026-03-10 | 120 B / 12.8 B active | 262 k | 74.8 + 0.50 GiB | **A** | ~108–114 GiB | **69.9 tok/s @1k, TTFT 126 ms** (measured, this card) | ⚠️ b12x kernels merged (PR #40082, 2026-05-20) but **broken under TP/PP** (#47365) — TP=1 only | OpenMDW-1.1 | 25 |\n| 7 | **Mistral Small 4 (119B-2603) FP8** | 2026-03-16 (mistral.ai/news) | 119 B / 6.5 B active | 256 k | 112.6 + 2.8 GiB (MLA) | **B** | 38 GiB @0.80 / 57 GiB @0.70 | none | ⚠️ MoE **and** MLA — both sm_120 fallback paths. Not on the official supported-HW list | Apache-2.0 | 20 |\n| 8 | Qwen3.5-122B-A10B | 2026-02-24 | 122 B / 10 B | 262 k | 61–65 GB NVFP4 (unofficial quant) | A/B | ~65–95 GiB | — | ⚠️ Marlin fallback | Apache-2.0 | 32 |\n| 9 | Nemotron-3-Nano-Omni-30B-A3B-NVFP4 | 2026-04-28 | 30 B / 3 B, multimodal | 131 k | 20.87 + 0.375 GiB | **A** | ~153 GiB | 269.8 tok/s — **but via TensorRT-LLM, not vLLM** | ⚠️ needs `--moe-backend triton` | NVIDIA open | ~14 (text sibling) |\n| 10 | DiffusionGemma-26B-A4B-it | 2026-06-09 | 25.8 B / 3.8 B | 256 k | 48.10 + 5.20 GiB | **A** | ~133 GiB | none; sm_120 boot **unproven** | ⚠️ head_dim 256 breaks FlashInfer (#40677) → `TRITON_ATTN` | Gemma/Apache | Google's own card: −5 to −19 pts vs its AR parent |\n\n**Ruled out with reasons** (so they don't get rediscovered): Nemotron-3-Ultra-550B (352 GB NVFP4 vs 191 GiB — 1.85× shortfall); DeepSeek-V4-Flash-DSpark (same weights **+7 GB** for the draft head — a latency win, never a VRAM answer); EXAONE-4.5-33B (**licence blocks commercial use**); Mistral Medium 3.5 (dense 128 B = worst latency profile here, non-Apache licence); Laguna-XS/S, North-Mini-Code (code-domain mismatch); Granite 4.1-30b (dominated by Qwen3.6-27B at the same size); Intern-S2 (\"Preview\", no validation); Ling-2.6-flash (**HF date not corroborated by any official post**; untravelled `bailing_hybrid` arch); everything ≥201 B (Step-3.7, Command-A-Plus, MiniMax-M3, GLM-5.2, Kimi K2.7/K3, Hy3, MiMo-V2.5, LongCat-2.0, DeepSeek-V4-Pro).\n\n**Meta-finding:** Meta has published **no open weights since 2025-04** (15 months). Microsoft's Fara1.5-27B and AI2's tmax-27b are both *Qwen finetunes* per their own `base_model` fields. Qwen3.5/3.6 has become the substrate other labs build on — which is independent corroboration for picking it directly.\n\n---\n\n## 3. Top picks\n\n### Tier A: **Qwen3.6-27B-FP8**, TP=1, one card\n\n```\n--tensor-parallel-size 1 --gpu-memory-utilization 0.55 --max-model-len 131072\n--max-num-seqs 16 --kv-cache-dtype fp8_e4m3 --mamba-cache-dtype float16\n--mamba-ssm-cache-dtype float16 --language-model-only --enable-prefix-caching\n--disable-custom-all-reduce   (+ enable_thinking:false baked into the chat template)\n```\nUses ~49 GiB on card 0 → **43 GiB free there plus card 1 entirely untouched (95.6 GiB)**. MageZero (12.53 GB) + gemma-4-12B QLoRA (11.6 GB) = 22.5 GiB fit on card 1 alone with 73 GiB spare.\n\nWhy it wins:\n- **Smallest measured quality gap of any Tier-A option** (AA 37 vs 40; MMLU-Pro 86.2 vs 86.4, GPQA-D 87.8 vs 88.1). The losses concentrate in agentic coding and frontier math — irrelevant to \"pick a legal action from a numbered menu.\"\n- **It is dense.** Every sm_120 landmine in this survey lives in the FP4/FP8 **MoE grouped-GEMM** path (vLLM #31085 open since 2025-12-20, #44688, CUTLASS #3096 silent-garbage-output). A dense FP8 model routes around all of it.\n- **TP=1 is simultaneously the VRAM-optimal and the latency-optimal choice** — for a 27 B, NCCL all-reduce over PCIe costs more than it saves. Freeing the second card is not a trade-off against speed.\n- Apache-2.0, 3.3 months old, and someone has published a working config for *literally* dual RTX PRO 6000 Blackwell (`theogravity/dual-rtx-6000-blackwell-qwen3.6-27b-fp8`).\n- The verdict is robust to quantization: even unquantized BF16 + BF16 KV is 72.0 GiB on one card. There is no configuration where this fails the 30 GB bar.\n\n**What would have to be true for this to be wrong:** (a) 27 B *active* params is memory-bandwidth-bound — a ~60-token answer is 0.44–0.60 s of decode plus 0.3–0.6 s cold prefill, so end-to-end lands **~0.75–1.2 s: it straddles your budget rather than clearing it.** If measured p95 exceeds ~1.2 s, drop to **Qwen3.6-35B-A3B-FP8** (3 B active, 170–208 tok/s measured on this exact card) and accept AA 32. (b) If thinking mode leaks onto the real-time path you reproduce commit 200ecff's 6.1 s failure exactly — both hardware-matched sources for this model ship a custom chat template with thinking off by default, and so must you. (c) The working path is a **nightly** vLLM container (`cu130-nightly`); pin a digest, not a floating tag.\n\n### Tier B: **MiniMax-M2.7-NVFP4** — but I would not take Tier B at all\n\nIt is the **only candidate in this entire sweep whose measured intelligence does not regress** against the incumbent (AA 38 vs DeepSeek 37 at High Effort, 40 at Max). It spans both cards at ~130–135 GB, leaving ~56–61 GB free — roughly 2× the requirement.\n\n**Why I still rank it below Tier A:** its **release day could not be verified from a primary source** (NVIDIA's deployment blog says 2026-04-11, Pandaily 04-12, HF repo shows 04-20, and MiniMax's *own* news page rendered 2026-03-18); its licence is a custom \"other\", not MIT/Apache; and it is NVFP4 MoE, i.e. the exact path that is broken on sm_120 — you would run it on the Marlin W4A16 fallback, which dequantizes FP4 to FP16 and pays FP4's accuracy cost for none of its speed. The only sm_120 datapoint for that path is 50.5 tok/s. Against a hard 0.9–1.1 s budget that is a real risk with no measurement behind it.\n\n**Given that a 27 B on one card gets you to AA 37, Tier B is hard to justify.** If you take it anyway, take it because you refuse any quality regression, and benchmark the Marlin path first. (Also: leave MTP off — the one published SM120 report found it *degraded* Marlin throughput by 22%.)\n\n---\n\n## 4. What is not known — read this before deciding\n\n1. **Nobody has run any of these on your prompts.** Every quality claim above is a public benchmark. Your own `CLAUDE.md` says it plainly: public scores do not predict mtgacoach quality, which is why `tools/eval/` exists. \"Comparable to DeepSeek V4 Flash\" is an **inference**, not a measurement. Treat AA 37-vs-40 as a prior, not a result.\n2. **The comparison baseline is the wrong DeepSeek.** AA's 40 is V4 Flash at *Max reasoning effort*. Your real-time path deliberately disables extended reasoning (commit 200ecff). DeepSeek's own model card publishes the non-think column: **MMLU-Pro 83.0 (not 86.4), GPQA-Diamond 71.2 (not 88.1)** — a 16.9-point GPQA collapse. Meanwhile every candidate's published number is *its* thinking-on score. **The like-for-like, thinking-off comparison does not exist in public data for any pair here, in either direction.** The apparent regressions are probably overstated; by how much is unmeasured.\n3. **The survey's arithmetic needed correcting in several places**, which is itself a caution: Qwen3.6-27B's KV was overstated 4.1× (only 16 of 64 layers are full-attention) and its DeltaNet state cache — the term that actually OOMs in the field at ~76 MiB/seq — was omitted entirely; Gemma 4's KV was overstated ~12× (50 of 60 layers are sliding-window-capped at 1024); Puzzle-75B's weights were understated by 8–9 GB (a HF *parameter* count was read as a *byte* count). Verify VRAM from safetensors dtype tables, never from blog round numbers.\n4. **sm_120 kernel status is in active churn** — PR #40082 merged 2026-05-20, a TP regression (#47365) was filed ~2026-06-30, #31085 is still open. Anything asserted here about NVFP4 MoE could change within weeks, in either direction.\n5. **Unverified release dates, flagged:** MiniMax-M2.7 (day only; month solid), Ling-2.6-flash, Laguna-XS/S-2.1, North-Mini-Code-1.0, Mistral Medium 3.5. **Stale-but-listed:** Nemotron-3-Nano-30B (2025-12-20, 7.3 months, superseded twice inside its own family), Qwen3.5 family (2026-02).\n6. **No candidate is measured on this hardware for latency except three:** Qwen3.6-27B (100–137 tok/s), Qwen3.6-35B-A3B (170–208, or 72 misconfigured), Nemotron-3-Super-120B (69.9 tok/s, TTFT 126 ms). Everything else is arithmetic.\n7. **There is no room to A/B without stopping DeepSeek.** It occupies 94.5 GB × 2 with ~2 GB free, and 0.95 is a floor. Any test needs a window. See below.\n\n---\n\n## 5. Concrete next step — safe, out-of-band, never edits the live container\n\nThe 13-minute outage came from retuning the *live* container's flags on reasoning. This plan never modifies the live container; it stops it cleanly, runs a **separate throwaway container on a different port and a different card**, and restores with one command. Rollback is `docker start`.\n\n**Phase 0 — zero risk, do now, service stays up (no VRAM, no config change):**\n\n```bash\n# 0a. Capture ≥30 real coach prompts. Set in the shell that launches the desktop app, then play.\nexport MTGACOACH_PROMPT_DUMP_PATH=\"$HOME/.arenamcp/eval_prompts.jsonl\"\npython -m arenamcp.desktop\n\n# 0b. Record the DeepSeek BASELINE while it is still up. run.py is idempotent.\npython -m tools.eval.run \\\n  --prompts \"$HOME/.arenamcp/eval_prompts.jsonl\" \\\n  --backend \"online:deepseek-v4-flash\" \\\n  --license-key \"$MTGACOACH_LICENSE_KEY\"\n\n# 0c. Pull weights to disk only — ~31 GB, no GPU touched.\nhuggingface-cli download Qwen/Qwen3.6-27B-FP8 --local-dir /models/Qwen3.6-27B-FP8\n\n# 0d. Confirm the vLLM image and CUDA toolkit (flashinfer JIT fails on sm_120 with PyPI CUDA fragments).\nssh joshu@10.0.0.100 \"docker inspect --format '{{.Config.Image}}' <live-vllm-container>; nvcc --version\"\n```\n\n**Phase 1 — needs a short window. Stop, do not reconfigure.**\n\n```bash\n# 1. Stop the incumbent. Do NOT edit its flags, do NOT docker rm. This is the whole safety property.\ndocker stop <live-vllm-container>\n\n# 2. Candidate on CARD 1 ONLY, port 8002 (live port untouched), --rm so it leaves no trace.\ndocker run --rm --name vllm-qwen36-test \\\n  --gpus '\"device=1\"' -p 8002:8000 -v /models:/models \\\n  vllm/vllm-openai@sha256:<PINNED_DIGEST_cu130_nightly> \\\n  --model /models/Qwen3.6-27B-FP8 --served-model-name qwen36-27b-test \\\n  --tensor-parallel-size 1 --gpu-memory-utilization 0.55 \\\n  --max-model-len 32768 --max-num-seqs 16 \\\n  --kv-cache-dtype fp8_e4m3 \\\n  --mamba-cache-dtype float16 --mamba-ssm-cache-dtype float16 \\\n  --language-model-only --enable-prefix-caching \\\n  --disable-custom-all-reduce --reasoning-parser qwen3\n\n# 3. PROVE THE ACTUAL REQUIREMENT: card 0 must be free. Start MageZero + the QLoRA job on card 0\n#    (CUDA_VISIBLE_DEVICES=0) while the candidate serves, then:\nnvidia-smi --query-gpu=index,memory.used,memory.total --format=csv\n#    Pass condition: card 1 ≈ 53 GiB used, card 0 holds both training jobs, ≥30 GB still free overall.\n\n# 4. Latency, concurrency 1, warm, thinking OFF, on a REAL ~6.3k-token prompt.\n#    Pass condition: p95 end-to-end ≤ ~1.2 s. Confirm decode lands near 100–137 tok/s, not ~40.\ncurl -s -w '\\n%{time_starttransfer} TTFT %{time_total} TOTAL\\n' \\\n  http://localhost:8002/v1/chat/completions -H 'Content-Type: application/json' -d @- <<'JSON'\n{\"model\":\"qwen36-27b-test\",\"max_tokens\":80,\n \"chat_template_kwargs\":{\"enable_thinking\":false},\n \"temperature\":0.7,\"top_p\":0.8,\n \"messages\":[{\"role\":\"user\",\"content\":\"<PASTE ONE CAPTURED 21,500-CHAR PROMPT>\"}]}\nJSON\n\n# 5. Score the candidate against the baseline captured in 0b.\npython -m tools.eval.run \\\n  --prompts \"$HOME/.arenamcp/eval_prompts.jsonl\" \\\n  --backend \"openai-compatible|http://10.0.0.100:8002/v1|qwen36-27b-test\"\n\n# 6. RESTORE — one command, original config, unmodified.\ndocker stop vllm-qwen36-test && docker start <live-vllm-container>\n\n# 7. Judge + report AFTER DeepSeek is back (the default judge is online:deepseek-v4-flash).\npython -m tools.eval.judge && python -m tools.eval.report\n```\n\n**Gate on your own documented thresholds, not on MMLU:** **legality ≥ 4.5** (below this it hallucinates illegal actions and cannot be the autopilot path), and **correctness/reasoning within 0.5 of the incumbent**. If legality passes and p95 ≤ 1.2 s, cut the gateway over. If latency fails, retry with `Qwen3.6-35B-A3B-FP8` (same recipe, `--max-num-seqs 512`, no `--language-model-only`) before considering anything else.",
    "verified": [
      {
        "name": "Nemotron 4 — CONFIRMED NON-EXISTENT as a 2026 model (name also collides with a stale 2024 release)",
        "date_confirmed": "CONFIRMED with one correction. (a) The 2026 \"Nemotron 4 family\" has NO release date — it was announced only, on 2026-03-16 at GTC, and no weights exist as of 2026-07-30. (b) CORRECTION to the survey agent: \"Nemotron-4\" is a REUSED NAME. Weights literally called Nemotron-4 DO exist and shipped 2024-06-12 (nvidia/Nemotron-4-340B-Base and -Instruct, arXiv 2406.11704). That is ~25 months old — stale by a wide margin — and 340B dense, physically unfittable on this pair. So \"not released\" is right for the model the owner means, but wrong as a literal statement about the name.",
        "date_source": "PRIMARY: NVIDIA newsroom press release 2026-03-16, \"NVIDIA Launches Nemotron Coalition of Leading Global AI Labs to Advance Open Frontier Models\" (nvidianews.nvidia.com + investor.nvidia.com mirror + GlobeNewswire 2026/03/16). Fetched the release directly; it contains EXACTLY ONE sentence naming Nemotron 4: \"The first model built by the coalition will underpin the upcoming NVIDIA Nemotron 4 family of open models.\" No sizes, no parameter counts, no timeline, no availability date. Checked NVIDIA newsroom through July 2026: the most recent Nemotron press release is 2026-07-15 (Japanese enterprises building on Nemotron open models) — a customer-adoption story that re-lists the coalition members and announces no base model. HuggingFace model search for \"nemotron-4\" sorted by created returns only the Nemotron-3 series (Nano/Super/Ultra) plus the legacy 2024 entries; zero 2026-created Nemotron-4 repos. Legacy date primary source: nvidia/Nemotron-4-340B-Instruct model card (published 2024-06-12, trained Dec 2023–May 2024) and arXiv:2406.11704.",
        "stale": true,
        "fits_verdict": "DOES_NOT_FIT",
        "vram_math": "No arithmetic is possible for the 2026 family: NVIDIA has published no parameter count, no architecture, no context length, no quantization. Any VRAM number for it would be fabricated.\n\nFor the legacy Nemotron-4-340B (the only thing bearing this name with real weights), the math is decisive against the pair (2 x 95.6 GiB = 191.2 GiB usable ~= 205 GB):\n- Weights, FP8 (1 B/param): 340e9 x 1 B = 340 GB. That is 1.66x the ENTIRE pair. Fails on weights alone, before any KV cache.\n- Weights, NVFP4/INT4 (0.5 B/param): 340e9 x 0.5 B = 170 GB, plus scales/embeddings/lm_head typically kept at higher precision -> realistically ~180-190 GB. Against ~205 GB total that leaves ~15-25 GB, i.e. ALREADY below the >=30 GB headroom requirement with zero KV cache allocated.\n- KV cache at 128k, FP16: config is 96 layers, hidden 18432, 96 heads -> head_dim 192, 8 KV heads (GQA). Per token = 2 (K+V) x 96 layers x 8 kv_heads x 192 head_dim x 2 B = 589,824 B = 0.5625 MiB/token. At 131,072 tokens = ~72,000 MiB = ~70.3 GiB for ONE sequence. FP8 KV halves it to ~35 GiB — still unaffordable.\n- Total at the most favorable named quant (NVFP4 weights + FP8 KV, single 128k sequence): ~180 GB + ~38 GB = ~218 GB vs ~205 GB capacity. Negative headroom. Fails.\nNeither TIER A (one card) nor TIER B (both cards, >=30 GB free) is reachable. For reference, the incumbent DeepSeek V4 Flash already consumes 94.5 GB per card measured, so there is no world in which a 340B dense model improves on that.",
        "vllm_ready": "N/A for the 2026 family — there is no architecture to support, so no vLLM support can exist or be tested. For the legacy 340B: vLLM does carry a Nemotron architecture family in mainline, but the point is moot since the weights cannot be loaded on 191.2 GiB regardless of kernel support. Separately worth grounding: sm_120 vLLM support is NOT the blocker on this host — the owner's own measured state (DeepSeek V4 Flash serving today with --tensor-parallel-size 2 at 0.95 utilization on these exact cards) is direct empirical proof that mainline vLLM runs on sm_120 Blackwell. Any sm_120 concern for a replacement candidate is model-architecture-specific (custom kernels, FP4 recipes), not a blanket platform gap.",
        "quality_vs_deepseek": "NONE — and I will not assert parity from vibes. There are zero benchmarks for the 2026 Nemotron 4 family because there is no model: no MMLU, no agentic evals, no head-to-head, not even a claimed number in NVIDIA's own press release. The legacy Nemotron-4-340B (2024-06) predates DeepSeek V4 Flash by roughly two years and has no shared modern eval surface with it; comparing them would be meaningless even if it fit. If the owner needs an NVIDIA-family candidate today, the only real options are the Nemotron 3 family that actually shipped (Nano 30B-A3B, Nano 4B, Super 120B-A12B, Ultra 550B-A55B, Labs-3-Puzzle-75B-A9B; Ultra landed on HF 2026-06-04, NVFP4 checkpoints published) — evaluate those, not this. Nemotron-3-Super-120B-A12B-NVFP4 in particular is worth a separate verification pass: ~120B total at NVFP4 is roughly 60-65 GB of weights with only 12B active, which is the shape that could plausibly hit TIER B on this pair with the ~0.9-1.1s latency budget. That is a lead, not a claim — it needs its own arithmetic and its own sm_120 check.",
        "reasoning": "The survey agent's core conclusion is CORRECT and I confirm it from primary sources: there is no Nemotron 4. It is vapor. Drop it from the candidate list entirely — it cannot be planned around, benchmarked, or scheduled, because NVIDIA has committed to nothing beyond the word \"upcoming\" in a single sentence four and a half months ago.\n\nThree adversarial findings the survey agent missed or under-stated:\n\n1. NAME COLLISION (correction). The agent said \"no weights exist.\" Literally false: nvidia/Nemotron-4-340B-Base and -Instruct are real, downloadable, and dated 2024-06-12 (arXiv 2406.11704). This matters operationally — a careless search for \"Nemotron 4 VRAM requirements\" will surface 2024 340B deployment guides and could be mistaken for the coalition model. This is the SAME failure mode as the Llama 4 Scout mistake: a real artifact with a recent-sounding name that is actually 25 months stale. It also fails on VRAM by a factor of ~1.7x even at NVFP4, so both readings of \"Nemotron 4\" converge on DOES_NOT_FIT.\n\n2. FABRICATED CORROBORATION IN THE WILD. Searching for a coalition timeline surfaced confident claims from SEO blogs (digitalapplied.com, kalinga.ai) that \"the coalition's 30B and 70B releases are expected mid-to-late 2026\" and that \"the coalition's first joint release, Nemotron-7B-Collab, achieved state-of-the-art results for its parameter class.\" I checked both. Nemotron-7B-Collab DOES NOT EXIST — a HuggingFace search returns only OpenCodeReasoning-, OpenReasoning-, AceReason-, OpenMath-, and AceMath-RL-Nemotron-7B, all unrelated Qwen/DeepSeek derivatives. And NVIDIA's actual press release gives no sizes and no timeline, so \"30B and 70B mid-to-late 2026\" has no primary basis. These are AI-generated slop pages. If a future agent searches this topic and does not check HuggingFace directly, it will come back with a fake model and a fake roadmap. Flagging explicitly so this does not get relayed as fact.\n\n3. VERDICT CHOICE. I chose DOES_NOT_FIT over UNVERIFIABLE deliberately. UNVERIFIABLE would imply \"gather more data\" — wrong signal. The actionable truth is that this candidate is non-deployable today and for an unknown period, and the only weights carrying the name fail the VRAM arithmetic decisively. The owner needs it removed from consideration, not queued for follow-up.\n\nNet: this verification consumed a candidate slot and returned nothing deployable. The useful residue is the Nemotron 3 family (real, June 2026, NVFP4 checkpoints published), specifically Super-120B-A12B, which should get its own verification pass with real arithmetic."
      },
      {
        "name": "NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4",
        "date_confirmed": "2026-03-10 — CONFIRMED, survey agent was right. HuggingFace API createdAt = 2026-03-10T18:33:04.000Z; NVIDIA official blog published 2026-03-11. Model is ~4.7 months old, comfortably past the 2025-12 staleness threshold. No listicle inference involved — this is the repo's own creation timestamp. (Repo lastModified 2026-05-01; linked tech reports are arXiv 2512.20848 / 2512.20856, i.e. Dec-2025 research predating the March release — that is the research, not the model drop.)",
        "date_source": "PRIMARY: https://huggingface.co/api/models/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4?expand[]=createdAt -> createdAt \"2026-03-10T18:33:04.000Z\". CORROBORATING PRIMARY: https://blogs.nvidia.com/blog/nemotron-3-super-agentic-ai/ dated 2026-03-11.",
        "stale": false,
        "fits_verdict": "TIER_A",
        "vram_math": "CARD: 97,887 MiB = 95.593 GiB each; 2 cards = 191.19 GiB.\n\nWEIGHTS (quantization = ModelOpt MIXED checkpoint: NVFP4 4-bit routed-expert weights + FP8-E4M3 backbone/attention/shared-expert + BF16 embeddings/latent/MTP layers — NOT uniform NVFP4). Three independent primary measures, all agreeing within 0.05 GiB:\n (a) Sum of the 17 actual safetensors shard file sizes = 80,317,948,856 B = 74.80 GiB. (I recomputed this by hand — an automated summarizer returned 84.92 GB for this same list, which is arithmetically wrong; the true sum is 80.32 GB.)\n (b) HF dtype table: U8 56,382,455,808x1 + F8_E4M3 11,873,353,728x1 + BF16 6,020,553,728x2 + F32 20,992x4 = 80,297,000,960 B = 74.78 GiB.\n (c) usedStorage = 80,351,660,383 B = 74.83 GiB.\n=> WEIGHTS = 74.8 GiB. Survey agent's 80.3 GB figure is CORRECT in decimal GB, but it then wrote \"~82-84 GB total on one card\" while silently mixing GB and GiB against a GiB-denominated card. In GiB the weights are 74.8, not 80.3 — which makes the fit MORE comfortable than reported.\n\nKV CACHE — derived from config.json, not from a blog. hybrid_override_pattern is 88 chars = 40 Mamba + 40 MoE + ONLY 8 real attention layers. num_key_value_heads=2, head_dim=128 (very narrow GQA).\n bytes/token = 8 layers x 2 (K+V) x 2 heads x 128 dim x dtype\n fp8:  4,096 B/tok -> 128k ctx = 0.50 GiB per sequence\n bf16: 8,192 B/tok -> 128k ctx = 1.00 GiB per sequence\n Mamba recurrent state (fixed per seq, context-INDEPENDENT): 40 layers x ~4.16 MiB fp32 = 0.16 GiB/seq.\nKV at 128k is ~1/30th of a conventional dense 120B. The survey's \"KV is unusually cheap\" claim is CONFIRMED and I can now show why.\n\nONE-CARD TOTAL @ 128k, fp8 KV:\n 1 concurrent seq: 74.80 + 0.50 + 0.16 + ~2.0 (CUDA ctx + activations) = 77.5 GiB\n 8 concurrent seqs (recipe --max-num-seqs 8): 74.80 + 4.00 + 1.30 + ~3.5 = 83.6 GiB\n\nHEADROOM vs the >=30 GB requirement:\n 1 seq: card0 free 18.1 GiB + card1 free 95.6 GiB = 113.7 GiB free (3.9x the requirement)\n 8 seqs: card0 free 12.0 GiB + card1 free 95.6 GiB = 107.6 GiB free (3.7x)\nMageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.13 GB = 22.5 GiB -> both fit on card 1 ALONE with 73.1 GiB still spare. Card 0 never has to be touched.\n\nINDEPENDENT CORROBORATION: a field report on this exact GPU states \"with MTP disabled, vLLM starts serving with roughly 77GB VRAM footprint\" on a 96GB RTX PRO 6000 — matching my computed 77.5 GiB single-stream figure almost exactly.\n\nTWO HARD OPERATIONAL FLOORS (both are the failure mode you already hit with DeepSeek at 0.75):\n 1. Weights are 78.3% of one card. --gpu-memory-utilization must stay above ~0.82 or it OOMs during WEIGHT LOAD. 0.92 -> 87.9 GiB budget (OK); 0.85 -> 81.3 GiB (OK); 0.75 -> 71.7 GiB, which is BELOW the 74.8 GiB of weights and will OOM exactly as DeepSeek did. Do not retry 0.75 here.\n 2. MTP/speculative decoding MUST be off. The recipe enables it, but it consumes 20GB+ at startup and OOMs even at 0.95 on a 96GB card. 74.8 + 20 = 94.8 GiB > any achievable budget. Disabling it is what produces the 77 GiB figure above.\nVerdict: TIER A is not marginal — it passes by ~3.7x even at 128k with 8 concurrent streams, and your real need is 6.3k at concurrency 1.",
        "vllm_ready": "YES for the Tier-A (TP=1) configuration — but the survey agent's kernel analysis is OUT OF DATE and I am correcting it in the model's favour, with a new restriction attached.\n\nWHAT THE SURVEY GOT WRONG: it reported that native NVFP4 MoE on sm_120 is unusable and you are stuck on a Marlin W4A16 fallback, citing vLLM #31085 (Dec 2025) and CUTLASS #3096 (Mar 2026). That was true then. Since then, vLLM PR #40082 (\"Integrate flashinfer b12x MoE and FP4 GEMM kernels for SM120/121\") was MERGED on 2026-05-20, explicitly targeting RTX PRO 6000 Blackwell + DGX Spark. Native NVFP4 MoE and FP4 GEMM are available today via --moe-backend / --linear-backend (b12x / flashinfer_cutedsl_sm12x).\n\nNEW RESTRICTION THE SURVEY DID NOT HAVE — and it happens to be harmless for you: vLLM issue #47365 reports the flashinfer_b12x MoE emits empty/garbage output when sharded across 2 GPUs under either tensor-parallel OR pipeline-parallel on sm_120 (regression between nightlies dev552/dev601, ~2026-06-30). SINGLE-GPU IS CORRECT. Tier A is TP=1, so you land squarely in the working configuration — but this permanently forecloses the survey's suggested \"if you ever go 2-card\" fallback. On this model, 1 card is not just preferred, it is the only validated b12x path.\n\nSTILL OPEN: vLLM #31085 remains open — the backend auto-selection logic still only checks the SM100 family, so sm_120 silently falls back to Marlin unless you pass --moe-backend EXPLICITLY. Do not rely on autodetection; verify from the startup log which backend was actually chosen.\n\nVRAM IS UNAFFECTED EITHER WAY: Marlin W4A16 keeps weights 4-bit packed in memory and dequantizes only inside the GEMM, so the 74.8 GiB figure and the Tier-A verdict hold regardless of which kernel path you end up on. The kernel question is a throughput question, not a fit question.\n\nREQUIREMENTS: vLLM >= 0.24.0 (v0.25+ advisable to pick up PR #40082), --trust-remote-code (custom_code model, nemotron_h), --mamba-cache-mode align, --mamba_ssm_cache_dtype float32, --kv-cache-dtype fp8, --reasoning-parser nemotron_v3, and MTP disabled. Also avoid --reasoning-config: vLLM issue #39103 reports it conflicts with the nemotron_v3 parser and makes content always null with an unbounded thinking trace — which would be a silent product-killer on your path.\n\nONE MORE CORRECTION: the survey (and NVIDIA marketing) claim 1M context. The shipped config.json says max_position_embeddings = 262144. Irrelevant to your 6.3k need, but it shows the survey took a marketing number over the config. Likewise the claim that this was \"pre-trained natively in NVFP4, not post-training-quantized\" is contradicted by the repo's own metadata: HF tags include \"modelopt\" and \"8-bit\", and config.json carries a ModelOpt per-layer PTQ quantization_config across all 88 layers. Treat this as a quantized checkpoint with normal PTQ quality loss, not a lossless native-FP4 train.",
        "quality_vs_deepseek": "CONFIRMED REGRESSION, and it is large. I independently loaded the Artificial Analysis head-to-head page the survey cited and it reports exactly what the survey claimed: Artificial Analysis Intelligence Index — Nemotron 3 Super 120B A12B (Reasoning) = 25 vs DeepSeek V4 Flash (Reasoning, Max Effort) = 40. That is a 37.5% relative drop. Same page: output 154 tok/s vs 122 tok/s, TTFT 1.71s vs 1.47s, $0.26 vs $0.06 per 1M tokens. AA publishes no per-benchmark breakdown (MMLU-Pro/GPQA/AIME) for this pairing, so 25-vs-40 is the only real number available — I am not going to dress it up.\n\nBUT THE COMPARISON IS NOT APPLES-TO-APPLES WITH YOUR DEPLOYMENT, and this cuts both ways:\n - AA's 40 is DeepSeek V4 Flash at \"Reasoning, Max Effort\". Your own repo disabled extended reasoning on the real-time path (commit 200ecff, \"5.5x faster\"). So the 40 is measured in a mode you deliberately do not run. Your production DeepSeek is almost certainly scoring below 40.\n - Conversely the Nemotron 25 is also its Reasoning score, and you would likewise have to run it with /no_think.\n - No public benchmark measures either model on \"read a structured MTG game state and pick one action from a numbered menu\" — a task far below the ceiling these indices probe. Neither the survey nor I can assert parity, and neither can assert the gap transfers. Your tools/eval harness (real captured prompts, legality + correctness rubric) is the only instrument that can settle this. The legality dimension in particular is the one that matters and the one AA does not measure at all.\n\nTHE FINDING THE SURVEY MISSED, AND IT IS THE SERIOUS ONE — LATENCY ON *YOUR* HARDWARE:\nThe survey used AA's hosted 154 tok/s as a counterweight to the quality drop. That number is from datacenter Blackwell, not your card. A published benchmark of this exact configuration (Nemotron-3-Super-120B-A12B NVFP4 on 1x RTX PRO 6000 Blackwell, FP8 KV, no prompt caching, no speculative decoding) measures:\n - TTFT 126 ms at 1K context; decode 69.9 tok/s at 1K, 62.3 tok/s at 512K (remarkably flat — the Mamba hybrid paying off)\n - 100% success across 1.6K requests\nSo real decode on your hardware is ~70 tok/s, LESS THAN HALF the 154 tok/s the survey leaned on.\nYour budget, worked through: ~6.3k-token prompt -> TTFT ~0.3-0.5s (scaling 126ms at 1K). A 1-2 sentence answer is ~40-60 output tokens at ~68 tok/s = 0.59-0.88s. TOTAL ~0.9-1.4s. That is AT or slightly OVER your 0.9-1.1s budget, with zero margin — and only if the reasoning trace is fully suppressed. Reasoning IS controllable (/think and /no_think system-prompt directives, plus a reasoning_budget integer in extra_body), so this is achievable, but any leaked thinking trace of even 300 tokens adds ~4.4s and reproduces your 6.1s product failure.\nNet: the VRAM case is excellent, the quality case is a genuine documented regression, and the latency case is marginal-at-best rather than the comfortable win the survey implied.",
        "reasoning": "VERDICT: TIER_A on the VRAM question — confirmed, by a wide margin, from primary sources. But I would not green-light the swap on VRAM alone, because the two non-VRAM findings are worse than the survey portrayed.\n\nWHAT I CONFIRMED:\n1. Release date 2026-03-10 (HF repo createdAt) + NVIDIA blog 2026-03-11. Not stale. The survey agent did this one correctly and did not repeat the Llama-4-Scout listicle error.\n2. Weights = 74.8 GiB, triangulated three ways (shard file sum, dtype table, usedStorage) all within 0.05 GiB. I recomputed the shard sum by hand after an automated summarizer returned 84.92 GB for the same file list — the correct sum is 80.32 GB / 74.80 GiB. Always worth re-adding.\n3. KV is genuinely cheap, and I can now show the mechanism rather than assert it: only 8 of 88 layers are attention (from hybrid_override_pattern), with 2 KV heads x 128 dim. 0.50 GiB per sequence at 128k with fp8 KV.\n4. One card holds it at 77.5 GiB (1 stream) to 83.6 GiB (8 streams @128k), leaving 107-114 GiB free across the pair against a 30 GB requirement. Card 1 is untouched; MageZero + QLoRA (24.13 GB) fit there with 73 GiB spare. Corroborated by a field report of \"~77GB footprint with MTP disabled\" on the same GPU.\n\nWHERE I CORRECTED THE SURVEY IN THE MODEL'S FAVOUR:\n- Its sm_120 analysis was stale. vLLM PR #40082 merged 2026-05-20 brought native flashinfer b12x NVFP4 MoE + FP4 GEMM to sm_120/121. You are not condemned to Marlin. Caveat: b12x is broken under TP/PP on sm_120 (#47365) and works only single-GPU — which is exactly Tier A, but it means 2-card is off the table for this model.\n- Its unit handling was loose (80.3 GB weights compared against GiB cards). In GiB the fit is roomier than it claimed.\n\nWHERE I CORRECTED IT AGAINST THE MODEL:\n- Context is 262,144 per config.json, not 1M (marketing number).\n- \"Natively pre-trained in NVFP4\" is contradicted by the repo's modelopt tag and per-layer PTQ quantization_config. Assume normal quantization loss.\n- The decisive one: the survey offset the quality drop with AA's hosted 154 tok/s. On your actual card the measured decode is ~70 tok/s. Recomputing your latency budget with the real number puts a 6.3k-prompt/50-token-answer call at ~0.9-1.4s — at or over budget, with no margin, and only with reasoning fully suppressed via /no_think. The survey called latency \"the thing most likely to kill this\"; it was right, but it under-quantified the risk by using a hosted number.\n\nRECOMMENDATION: This is the only credible Tier-A candidate here and the VRAM engineering is sound — it genuinely frees an entire card. But it is a confirmed quality downgrade (AA 25 vs 40) bought for latency that is, on your hardware, probably no better than what you have. Do not swap blind. Gate it on your own tools/eval harness: capture ~30 real prompts, run Nemotron with /no_think and MTP off at --max-model-len 32768 and --gpu-memory-utilization 0.85, and compare correctness and especially LEGALITY against production DeepSeek, while measuring end-to-end TTFT+decode on the 6.3k prompts. If legality drops below 4.5 or p95 latency exceeds ~1.2s, the freed card is not worth it.\n\nCONFIDENCE: high on date and VRAM (primary sources, arithmetic shown, independently corroborated). Medium-high on vLLM readiness (kernel situation is in active churn — PR merged May, TP regression filed late June, roughly one month before today). Medium on quality, because the only public benchmark compares configurations neither of you would actually run."
      },
      {
        "name": "Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4",
        "date_confirmed": "2026-04-28 — CONFIRMED, and slightly more precise than the survey agent's \"2026-04\". Three primary sources agree: (a) the HF model card's own release table states \"Hugging Face 04/28/2026\" and \"Build.Nvidia.com 04/28/2026\"; (b) the official vLLM blog post announcing the model is dated 2026-04-28; (c) HF API lastModified 2026-05-05T14:35:03Z. The survey agent's repo createdAt of 2026-04-24 is consistent (repo staged 4 days before public announcement) though I could not re-read createdAt directly — the HF API dropped the field on my expand[] query. Age: 3 months and 2 days. NOT stale, and not a listicle-recency error: the model itself is genuinely from 2026-04, not an old model surfaced by a 2026 article.",
        "date_source": "Primary: HF model card release table on https://huggingface.co/nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4 (\"Hugging Face 04/28/2026\"); official vLLM blog post https://github.com/vllm-project/vllm-project.github.io/blob/main/_posts/2026-04-28-nemotron-omni.md (dated 2026-04-28); HF API lastModified 2026-05-05.",
        "fits_verdict": "TIER_A",
        "stale": false,
        "vram_math": "RESOLVED A DISCREPANCY FIRST: the model card says \"20.9 GB\" and the survey agent says \"22.4 GB\". These are the SAME number in different units, not a contradiction — I summed the three safetensors shards from the HF file tree (9,999,356,664 + 9,993,385,656 + 2,416,291,872 = 22,409,034,192 bytes) = 22.41 GB decimal = 20.87 GiB. Card uses GiB, survey agent used decimal GB. Both correct.\n\nCROSS-CHECK AGAINST params x bytes-per-param (did not accept the round number): quantization is NVFP4 mixed — routed MoE experts at NVFP4 (E2M1 + per-block FP8 E4M3 scales over 16-element groups + per-tensor FP32 global scale), Mamba in_proj/out_proj + shared experts + attention o_proj at FP8, vision/audio encoders and projectors kept BF16. Card states 4.98 effective bpw. 30B x 4.98 bits = 18.7 GB decimal for the LLM; + ~1.9B BF16 encoder/projector params x 2 B = ~3.7 GB -> ~22.4 GB. Arithmetic closes on the measured file size.\n\nKV CACHE AT 128k — this is where the survey agent was directionally right (\"under 2 GB\") but had no math. I pulled config.json. hybrid_override_pattern = \"MEMEM*EMEMEM*EMEMEM*EMEMEM*EMEMEM*EMEMEMEM*EMEMEMEME\" = 52 chars = num_hidden_layers 52, of which '*' (attention) = 6, 'M' (Mamba2) = 23, 'E' (MoE-MLP) = 23. Only SIX attention layers carry KV. With num_key_value_heads=2 (GQA), head_dim=128, --kv-cache-dtype fp8:\n  per token = 6 layers x 2 (K+V) x 2 heads x 128 dim x 1 byte = 3,072 B/token\n  @131,072 tok, one sequence = 402,653,184 B = 0.375 GiB\n  @6,300 tok (your real prompt), one sequence = 0.018 GiB\nFor scale: a dense 30B with 52 attention layers and 8 KV heads would need ~35 GiB at 128k. This is ~100x smaller. Context length is a non-issue for this architecture.\nReal per-sequence cost is instead the CONSTANT Mamba state (does not grow with context): mamba_num_heads 64 x mamba_head_dim 64 x ssm_state_size 128 x 23 layers = 12.06M elem = 48.2 MB/seq at fp32 (the sm_120 recipe pins mamba_ssm_cache_dtype float32). Concurrency, not context, drives memory here — the shipped --max-num-seqs 384 would cost 18.5 GB of Mamba state alone.\n\nSINGLE-CARD BUDGET (TP=1, --max-model-len 131072, --max-num-seqs 32 which is far beyond a one-user coach):\n  weights 20.87 + KV worst case 32x0.375 = 12.00 + Mamba 32x0.048 = 1.44 + CUDA graphs/activations/vision workspace ~3.5  =  37.8 GiB\n  At your ACTUAL 6.3k prompts: 20.87 + 0.58 + 1.44 + 3.5 = ~26.4 GiB.\n\nAGAINST 2 x 95.6 GiB (191.19 GiB total):\n  Card 0: 95.59 - 37.8 = 57.8 GiB free\n  Card 1: 95.59 - 0    = 95.59 GiB free (entirely untouched, TP=1)\n  TOTAL FREE = 153.4 GiB vs the >= 30 GB requirement. Passes by ~5x.\n  MageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.1 GB fits on card 1 alone with ~71 GiB still spare.\nTIER A confirmed: one card, second card completely freed.\n\nOPERATIONAL TRAP THE SURVEY AGENT MISSED: --gpu-memory-utilization is a CEILING vLLM claims, not what the model needs. Left at the 0.9 default it will allocate ~86 GiB and your headroom vanishes — this is exactly what elsung/blackwell-llm-toolkit measured on an RTX PRO 6000: \"VRAM usage @ 8k context: 88 GB\" for this very model. You must pin CUDA_VISIBLE_DEVICES to one card AND set --gpu-memory-utilization ~0.40. Note this is the mirror image of your DeepSeek problem: there 0.95 is a floor, here a low value is a ceiling you must impose.",
        "vllm_ready": "YES on sm_120 today, with two documented workarounds — survey agent was accurate here and I confirmed the triton flag is on the model card itself, not folklore.\n\nRequirements: vLLM >= 0.20.0 (pinned by both the vLLM blog and recipes.vllm.ai), --trust-remote-code (custom architecture NemotronH_Nano_Omni_Reasoning_V3, tagged custom_code), --kv-cache-dtype fp8 for the NVFP4 variant, TP=1.\nWORKAROUND 1 (verified on the HF model card AND recipes.vllm.ai): \"Due to a current bug with FlashInfer + RTX Pro, append: --moe-backend triton\". Applies directly to you.\nWORKAROUND 2: NVFP4 with TP>1 needs --moe-backend flashinfer_cutlass. Irrelevant at TP=1, which is what you want anyway.\nHardware support is explicit, not inferred: the model card's supported-GPU list names \"NVIDIA Blackwell (B200 SXM/NVLink, RTX Pro 6000 SE, DGX Spark, Jetson Thor, RTX 5090)\". Your exact card is listed.\n\nKNOWN DEFECT, still open: vLLM issue #31085 — SM120 (12,0) fails is_device_capability_family(100), so FP4 MoE backend selection falls through to Marlin and emits \"Your GPU does not have native support for FP4 computation\". Native NVFP4 sm_120 kernels ARE compiled in vLLM 0.13.0+; only the detection logic is wrong. No PR merged as of today. Impact is throughput, not correctness — and since the RTX Pro workaround forces --moe-backend triton anyway, the CUTLASS-vs-Marlin selection is largely moot on the MoE path.\n\nINDEPENDENT EMPIRICAL VALIDATION ON YOUR EXACT SILICON: elsung/blackwell-llm-toolkit ran this exact NVFP4 repo on an RTX PRO 6000 96GB (sm_120), validated 2026-05-11 — 24 ms median TTFT, 269.8 tok/s single-stream decode, 62,407 tok/s prefill at a 5,426-token prompt, 705 tok/s aggregate at N=4. CAVEAT I must flag: that run used TensorRT-LLM v1.3.0rc13 (moe_config backend CUTLASS), not vLLM. So the strongest sm_120 evidence is for a different serving stack than the one you run; the vLLM path is documented and supported but I found no third-party sm_120 vLLM benchmark for the Omni NVFP4 variant specifically.\n\nLATENCY against your hard 0.9-1.1s budget, using those measured numbers: 6,300-token prefill / 62,407 tok/s = 0.10s; a 60-token 1-2 sentence answer / 269.8 tok/s = 0.22s -> ~0.35s end-to-end. Comfortable. BUT reasoning is ON BY DEFAULT and the card recommends max_tokens=20480 for reasoning mode; at 269.8 tok/s even 1,500 reasoning tokens = 5.6s, which reproduces your 6.1s product failure almost exactly. You MUST launch with --default-chat-template-kwargs '{\"enable_thinking\": false}' and cap max_tokens ~1024 (the card's own non-reasoning guidance). Bonus: an NVIDIA developer-forum thread on 4x RTX PRO 6000 found that same flag also fixed long-context retrieval returning empty/hallucinated responses — thinking mode was the culprit there too.",
        "quality_vs_deepseek": "SEVERE REGRESSION, and the evidence situation is worse than the survey agent stated. The agent said there is \"no verified AA Intelligence Index score for the Omni variant specifically.\" Correct — but I checked further: there are NO published text-only benchmarks for this checkpoint AT ALL. I read both the NVFP4 and BF16 model cards; every reported number is vision/audio/video (MathVista_MINI, Charxiv Reasoning, OCRBenchV2, 9-benchmark multimodal mean). The NVFP4-vs-BF16 table (mean 65.43 vs 65.80, delta -0.38) measures QUANTIZATION DAMAGE on multimodal tasks — it says nothing about text reasoning ability and must not be read as a quality score.\n\nBest available proxy, and I verified both sides on the SAME index version: text-only sibling NVIDIA Nemotron 3 Nano 30B A3B Reasoning scores 14 on Artificial Analysis Intelligence Index v4.1; DeepSeek V4 Flash scores 40 on v4.1. A 26-point / ~2.9x gap. The survey agent's numbers check out exactly.\n\nVERBOSITY, your specific product hazard: AA flags Nemotron Nano as \"very verbose,\" 120M output tokens to run the index vs a 37M median for its cohort. DeepSeek V4 Flash is also flagged verbose (230M vs 100M median) but is measured at 122 output tok/s with 1.47s TTFT on AA's hosted endpoints — your local deployment already beats that. The Nemotron verbosity risk is real but fully controllable via enable_thinking=false + a hard max_tokens cap.\n\nI will not assert parity from vibes, and I will not assert the gap is smaller than it looks: your task (read ~6k tokens of structured game state, pick from a numbered legal-action menu, justify in 1-2 sentences) is closer to constrained extraction than to the open-ended agentic/coding work that dominates AA v4.1, so the real-world gap on YOUR prompts is plausibly narrower than 40-vs-14 implies. But that is a hypothesis, not a measurement. You already own the instrument to settle it: tools/eval/ with real captured prompts, judged on correctness/reasoning/conciseness/LEGALITY. Legality < 4.5 is your documented disqualifier and is exactly what a 14-scoring model is most likely to fail on a numbered-menu task.",
        "reasoning": "Every claim the survey agent made survived adversarial checking, and two of its numbers I improved on.\n\nCONFIRMED: (1) Release date is real and recent — 2026-04-28 from the model card's own release table and a same-day official vLLM blog post, not inferred from a listicle. 3 months old. (2) TIER A fit is real and enormous: 20.87 GiB of weights + a KV cache that is ~100x smaller than a dense 30B because only 6 of 52 layers are attention layers. One card, ~58 GiB spare on it, the entire second card free — 153 GiB total headroom against a 30 GB requirement. (3) sm_120 works today with the documented --moe-backend triton flag, and the card explicitly names \"RTX Pro 6000 SE\" as supported. (4) The quality regression is real and large.\n\nCORRECTIONS/ADDITIONS TO THE SURVEY: (a) The \"20.9 GB vs 22.4 GB\" apparent conflict is a GiB-vs-GB unit artifact, not an error — both are the same 22,409,034,192 bytes I summed from the file tree. (b) The survey's \"well under 2 GB KV\" was right but unquantified; the actual figure is 0.375 GiB for a full 131k sequence, and the survey missed that the dominant per-sequence cost is the CONSTANT 48.2 MB Mamba state — concurrency, not context, is the memory driver, so the shipped --max-num-seqs 384 would burn 18.5 GB needlessly. (c) The survey missed the biggest operational trap: --gpu-memory-utilization is a ceiling vLLM claims, and the one published RTX PRO 6000 run of this exact model measured 88 GB used at 8k context. Left at defaults this model would eat a whole card and deliver ZERO of the headroom that is the entire point of the swap. Pin CUDA_VISIBLE_DEVICES and --gpu-memory-utilization ~0.40. (d) The survey said \"disable reasoning\" as a footnote; it is not a footnote, it is a launch-blocking requirement — reasoning is ON by default with a recommended max_tokens of 20480, which at the measured 269.8 tok/s decode reproduces your 6.1s failure. The flag is --default-chat-template-kwargs '{\"enable_thinking\": false}', independently confirmed working in an NVIDIA forum thread where it also fixed long-context retrieval. (e) The strongest sm_120 benchmark for this model used TensorRT-LLM, not vLLM — the vLLM sm_120 path is documented and supported but not third-party-benchmarked for this variant.\n\nBOTTOM LINE ON THE ACTUAL DECISION: this model answers the VRAM question emphatically — it is the most headroom-generous candidate imaginable, freeing a whole card and then some. It does not answer the quality question, and the only proxy evidence available (AA 14 vs 40) points to a large intelligence drop, with zero published text benchmarks for this specific checkpoint. The multimodal capability is a genuine new option — feeding MTGA screenshots to the vision tower as a cross-check on log-derived state would be a new capability, consistent with your log-first doctrine since it would enrich rather than replace observation — but that is a different project from replacing your incumbent coach model. Recommendation: TIER A on fit, but do not swap on this evidence. Run tools/eval/ with >= 30 captured real prompts against online:deepseek-v4-flash as the baseline, with enable_thinking=false and max_tokens=1024, and gate on your own documented legality >= 4.5 threshold before touching production."
      },
      {
        "name": "NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4",
        "date_confirmed": "2025-12-20 (HF repo createdAt 2025-12-20T09:12:40.000Z); public announcement 2025-12-15. CONFIRMED — the survey agent's \"2025-12\" is correct, not a listicle inference. ~7.3 months old today (2026-07-30).",
        "date_source": "PRIMARY: https://huggingface.co/api/models/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4 → createdAt 2025-12-20T09:12:40.000Z, lastModified 2026-03-15T04:27:10.000Z, usedStorage 19,359,874,045 B, downloads 1,176,253, base_model nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16. CORROBORATING: NVIDIA newsroom announcement 2025-12-15; arXiv 2512.20848 \"Nemotron 3 Nano: Open, Efficient Mixture-of-Experts Hybrid Mamba-Transformer Model for Agentic Reasoning\"; NVIDIA technical report research.nvidia.com/labs/nemotron/files/NVIDIA-Nemotron-3-Nano-Technical-Report.pdf; AWS Bedrock GA notice dated 2025/12; OpenRouter lists release 2025-12-14. Multiple independent primary/first-party sources agree on mid-December 2025.",
        "fits_verdict": "TIER_A",
        "stale": false,
        "vram_math": "All weight figures derived from the HF safetensors dtype census (primary), not a blog round number.\n\nWEIGHTS (quantization: NVFP4, block size 16 — verified below):\n  U8      15,245,905,920 × 1 B = 15,245,905,920 B   (packed FP4 values, 2 per byte)\n  F8_E4M3  1,905,738,240 × 1 B =  1,905,738,240 B   (per-block scales)\n  BF16     1,078,212,032 × 2 B =  2,156,424,064 B   (embeddings/norms/Mamba params)\n  F32          7,916,416 × 4 B =     31,665,664 B\n  TOTAL                        = 19,339,733,888 B = 19.34 GB = 18.01 GiB\n\n  Cross-check 1: repo usedStorage 19,359,874,045 B − 19,339,733,888 B = 20.1 MB delta = tokenizer/config/index JSON. Consistent.\n  Cross-check 2: U8 × 2 = 30,491,811,840 FP4 values; 30,491,811,840 / 1,905,738,240 = exactly 16.0 → confirms NVFP4 block-16 scaling.\n  Cross-check 3: 30,491,811,840 + 1,078,212,032 + 7,916,416 = 31,577,940,288 ≈ 31.6B total params, matching NVIDIA's stated count. Params × bytes-per-param reconciles. The survey agent's 19.36 GB is CORRECT.\n\nKV CACHE AT 128k — much smaller than a dense model, and the survey agent understated how favorable this is.\nFrom config.json: num_hidden_layers=52, hybrid_override_pattern=\"MEMEM*EMEMEM*EMEMEM*EMEMEM*EMEMEM*EMEMEMEM*EMEMEMEME\" = 52 chars containing 6 '*' (attention), 23 'M' (Mamba-2), 23 'E' (MoE). ONLY 6 LAYERS HAVE A KV CACHE. num_key_value_heads=2, head_dim=128 (GQA 32→2).\n  per token = 2(K,V) × 2 kv_heads × 128 head_dim × 6 attn layers = 3,072 elements\n  FP8  (--kv-cache-dtype fp8): 3,072 B/token × 131,072 tok = 402,653,184 B = 0.375 GiB per 128k sequence\n  BF16:                        6,144 B/token × 131,072 tok = 0.750 GiB per 128k sequence\nMamba-2 recurrent state (constant per sequence, NOT per token):\n  SSM  64 heads × 64 head_dim × 128 state × 23 layers = 12,058,624 elem × 4 B = 48.2 MB\n  conv (4096 + 2×8×128) × 4 kernel × 23 layers ≈ 2.3 MB  →  ~48 MiB/sequence\n\nSINGLE-CARD BUDGET (--tensor-parallel-size 1), vs 95.6 GiB:\n  1 seq @128k FP8:  18.01 + 0.375 + 0.05 + ~2.0 (CUDA ctx/activations) = 20.4 GiB  → card 0 free 75.2 GiB\n  8 seq @128k FP8:  18.01 + 3.00  + 0.39 + ~2.0                       = 23.4 GiB  → card 0 free 72.2 GiB\n  Card 1: 95.6 GiB entirely free.\n  TOTAL FREE ≈ 168–171 GiB against a ≥30 GB requirement — passes by ~5.6x.\n  MageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.13 GB fits on card 1 alone with ~71 GiB spare.\n\nReal workload (~6.3k-token prompts) needs ~18 MiB of KV per request — the KV term is noise.\n\nDEPLOYMENT CAVEAT the survey agent missed: do NOT reuse --gpu-memory-utilization 0.95 from the DeepSeek config. At TP=1 vLLM would greedily claim ~90 GiB of KV cache on card 0 and starve the training jobs. Set roughly --gpu-memory-utilization 0.28 (or pin --kv-cache-memory explicitly) and CUDA_VISIBLE_DEVICES to a single card. Unlike DeepSeek V4 Flash where 0.95 is a floor, here it is pure slack.",
        "vllm_ready": "Runs on sm_120, but ONLY via the Marlin fallback — and the survey agent understated this. The nemotron_h hybrid Mamba-2 arch is supported in mainline vLLM (day-0 Nemotron 3 support, vllm.ai blog 2026-06-04), so loading is fine; the FP4 kernels are the problem. Confirmed defect cluster: vLLM #31085 (opened 2025-12-20, STILL OPEN) — SM120 missing from NVFP4/MXFP4 backend selection, silently falls back to Marlin; vLLM #33416 (closed, PR #33417) — root cause is is_device_capability_family(100) returning False for SM12.0, since RTX PRO 6000 reports 12.0 and sm_100a instructions are absent on workstation Blackwell; vLLM #33333 and #34452 (2026-02-12, this exact model on Blackwell Pro 6000, closed with no stated fix) — \"NvFp4 MoE backend 'FLASHINFER_CUTLASS' does not support the deployment configuration\". NOT IN THE SURVEY REPORT and more serious: flashinfer #2577 \"NVFP4 mm_fp4 GEMM broken on SM120 — all backends fail\", plus flashinfer #2723 / NVIDIA cutlass #3096 \"NVFP4 MoE CUTLASS Grouped Block-Scaled GEMM Produces GARBAGE OUTPUT\" on SM120 — silent numerical corruption, not a clean crash; FlashInfer's autotuner fails all ~72 SM120 tactics because templates are sm_100a-only. Fixed only by out-of-tree patches + compute_120f on CUDA 13.0, measured at 39 tok/s. WORKING CONFIG: VLLM_NVFP4_GEMM_BACKEND=marlin with VLLM_USE_FLASHINFER_MOE_FP4 and VLLM_FLASHINFER_MOE_BACKEND unset; community guidance (2026-03-08) is explicit — \"Use Marlin for production until compute_120f results are confirmed.\" Marlin is W4A16 (dequant FP4→BF16 on the fly), so the 18.01 GiB VRAM figure holds but native FP4 tensor cores are forfeited; at 3B active params batch-1 decode is bandwidth-bound so the cost is small. Key risk: following NVIDIA's own recommended env vars yields either a hard ValueError or silently wrong tokens — an unacceptable failure mode for a product that speaks advice aloud.",
        "quality_vs_deepseek": "Artificial Analysis Intelligence Index v4.1 (identical 9-eval suite, apples-to-apples): Nemotron 3 Nano 30B A3B (Reasoning) = 14, rank #34/130 vs DeepSeek V4 Flash (Reasoning, Max Effort) = 40, rank #12/99, and DeepSeek V3.2 (Reasoning) = 32. A 26-point gap — it scores 35% of the incumbent and below even the prior-generation DeepSeek. Survey agent's claim CONFIRMED. Honest limitation: AA's head-to-head page shows aggregate only, per-benchmark rows are \"No data available\" for both, and no public benchmark measures MTG game-state action selection, so I cannot decompose the gap — but I found zero evidence of parity anywhere and a 26-point aggregate is far beyond benchmark noise. Verbosity compounds it: 120M output tokens for the index vs ~37M median (3.2x), hosted TTFT 1.16s at 249.8 tok/s. This repo already shipped commit 200ecff to stop extended reasoning on the real-time path after a 6.1s product failure; adopting a 3.2x-verbose reasoning model reintroduces that exact defect. Reasoning can be disabled to recover latency, but AA 14 is the reasoning-ON score, so non-reasoning lands lower still.",
        "reasoning": "VERDICT: Fits TIER_A trivially. Should not be adopted. Fitting and being a suitable replacement are different questions, and this candidate passes the first decisively while failing the second.\n\n(1) DATE — verified from primary source, survey agent was accurate. HF API createdAt 2025-12-20T09:12:40Z, announcement 2025-12-15, arXiv 2512.20848. No listicle inference involved. Note on the `stale` flag: I set it false strictly per the field's definition (\"released before 2025-12\") — 2025-12-20 falls inside 2025-12, not before it. But the letter and the spirit diverge here and the owner should read this as a fail, not a pass: at 7.3 months old it is the oldest thing under consideration, roughly 2.5x the \"few months\" tolerance, and it has been superseded twice inside its own family (Nemotron 3 Nano Omni 2026-04; Nemotron 3 Super 120B A12B and Ultra 550B with vLLM day-0 support 2026-06-04). Its 4-month-newer, more capable siblings should be evaluated before it.\n\n(2) VRAM — verified independently and the survey agent was correct, in fact conservative. I reconciled params × bytes-per-param against the HF dtype census three ways (usedStorage delta of 20 MB = JSON files; scale-to-value ratio exactly 16.0 confirming NVFP4 block-16; total 31.58B matching NVIDIA's 31.6B). Weights are 18.01 GiB, not a blog round number. The survey agent quoted KV at 32k; I computed 128k as instructed and it barely moves the needle, because config.json's hybrid_override_pattern shows only 6 of 52 layers are attention (23 Mamba-2, 23 MoE) with 2 KV heads — 0.375 GiB per 128k sequence at FP8. Even 8 concurrent 128k sequences leaves ~168 GiB free across the pair against a 30 GB requirement. The entire second card stays empty; MageZero (12.53 GB) + gemma-4-12B QLoRA (11.6 GB) = 24.13 GB fits there with ~71 GiB spare. Context length is a non-issue: the model advertises 262,144 native (config max_position_embeddings), 1M in supported configs, and the real workload is 6.3k tokens.\n\n(3) vLLM/sm_120 — I found this to be materially worse than the survey agent described, and it is the strongest technical objection after quality. They characterized it as \"the recommended fast path is broken.\" The truth is a five-issue cluster spanning vLLM, FlashInfer, and CUTLASS, with the headline defect being SILENT GARBAGE OUTPUT from the SM120 NVFP4 grouped GEMM (cutlass #3096, flashinfer #2723), not merely a clean error. The lead vLLM issue #31085 is still OPEN as of today. The working configuration is the Marlin W4A16 fallback, which preserves the VRAM math but forfeits native FP4 tensor cores — acceptable for batch-1 decode at 3B active, but it means the headline NVFP4 performance story does not apply to this hardware.\n\n(4) QUALITY — this is what kills it. AA Intelligence Index v4.1 puts it at 14 against DeepSeek V4 Flash's 40 on the identical 9-eval suite; it does not even reach DeepSeek V3.2's 32. I was unable to find any per-benchmark decomposition or any domain-relevant head-to-head, and I am not going to assert parity from vibes — but a 26-point aggregate gap admits no charitable reading. Compounding it, AA flags 120M output tokens against a 37M median, and this repo has already shipped a fix (200ecff) for exactly the failure mode that verbosity causes on the real-time voice path.\n\nWHAT IT IS ACTUALLY GOOD FOR: the one genuine merit is latency headroom. 3B active params at 4-bit on a single card, with only 6 attention layers to grow KV across, would sit comfortably inside the 0.9-1.1s budget with room to spare. If a stronger candidate (e.g. Nemotron 3 Super 120B A12B) measures unacceptable TTFT, this becomes the speed-over-intelligence fallback — but even then, prefer the 2026-04 Nano Omni for recency, and prefer running this one with reasoning disabled to control verbosity, accepting a score below 14.\n\nRECOMMENDATION: reject as a DeepSeek V4 Flash replacement. Keep on a shortlist only as a latency escape hatch if better candidates fail the timing gate. The relevant question this candidate raises is whether a mid-sized 2026 model can hit AA ~30+ inside ~60 GiB on one card — that is where the search should go next."
      },
      {
        "name": "Qwen3.6-27B (Alibaba/Qwen)",
        "date_confirmed": "2026-04-21 (CONFIRMED, survey's \"2026-04\" is correct). HF API createdAt=2026-04-21T07:50:43Z for Qwen/Qwen3.6-27B; the FP8 repo Qwen/Qwen3.6-27B-FP8 createdAt=2026-04-21T07:51:33Z (53 seconds later — consistent with a coordinated official launch, not a backfill). Public announcement 2026-04-22. Age at 2026-07-30: ~3.3 months. Comfortably inside the owner's recency bar; nowhere near the Llama-4-Scout failure mode.",
        "date_source": "PRIMARY: HuggingFace API https://huggingface.co/api/models/Qwen/Qwen3.6-27B (createdAt 2026-04-21T07:50:43Z, lastModified 2026-04-24). Corroborated by the official Qwen/Qwen3.6-27B-FP8 repo createdAt 2026-04-21T07:51:33Z and the QwenLM/Qwen3.6 GitHub news entry (2026-04-22). Three independent official surfaces agree; no listicle was used to establish recency.",
        "stale": false,
        "fits_verdict": "TIER_A",
        "vram_math": "All figures derived from the HF safetensors dtype breakdown and config.json, not from blog round numbers. Card = 97,887 MiB = 95.59 GiB; pair = 191.19 GiB.\n\nWEIGHTS (named quant):\n- BF16: 27,781,427,952 params x 2 B = 55.56 GB = 51.75 GiB.\n- FP8 (official Qwen/Qwen3.6-27B-FP8): NOT 27.8 GB. The repo keeps 3,083,727,792 params in BF16 (vision tower, embeddings, MTP head, norms) and only 24,699,207,680 in F8_E4M3. Real total = 3.084e9x2 + 24.699e9x1 = 30.87 GB = 28.75 GiB. The survey's 27.8 GB is a naive params x 1 B and UNDERSTATES by 3.07 GB.\n- With --language-model-only (skips the ~0.5B vision encoder): ~29.9 GB = 27.8 GiB. This reconciles with the field-observed \"~27 GB\" on real dual-6000 hardware.\n\nKV CACHE — SURVEY IS WRONG BY 4.1x. This is a HYBRID model, not a uniform transformer. config.json has layer_types with full_attention_interval=4, i.e. 16 x (3 x Gated DeltaNet -> 1 x Gated Attention). Only 16 of 64 layers hold a growing KV cache; the survey used all 64.\n- Per token = 16 layers x 4 KV heads x 256 head_dim x 2 (K+V) = 32,768 elements => BF16 64 KiB/tok, FP8 32 KiB/tok. (Survey said 262 KiB/tok BF16 = 4.1x too high. Conservative direction, but wrong.)\n- At 128k (131,072 tok), one sequence: BF16 8.00 GiB, FP8 4.00 GiB. At the real 6.3k prompt: FP8 0.19 GiB.\n\nDELTANET STATE — SURVEY OMITTED THIS ENTIRELY, and it is the term that actually OOMs in the field. 48 DeltaNet layers, state = 48 v-heads x 128 x 128 + conv, fp16 mandatory (fp8 breaks correctness) => ~76 MiB PER SEQUENCE, constant regardless of context length. 16 seqs = 1.18 GiB; 256 seqs = 18.94 GiB. My independently derived 18.94 GiB figure exactly explains a real reported \"Mamba state cache OOM\" on an RTX PRO 6000 at max-num-seqs 256 — strong validation of the model.\n\nTIER A BUDGET (one card, TP=1, FP8 weights, FP8 KV, --language-model-only, 128k, max-num-seqs 16):\n27.82 (weights) + 4.0 (CUDA graphs/activations) + 1.18 (DeltaNet state) + 16.0 (KV pool) = 49.00 GiB => --gpu-memory-utilization 0.55 (52.6 GiB).\nThat 16 GiB KV pool holds 524,288 tokens = 4 concurrent seqs at full 128k, or 83 concurrent seqs at the real 6.3k prompt. Massively over-provisioned for a single-user voice coach.\nFREE: card0 43.0 GiB + card1 95.6 GiB (entirely untouched) = 138.6 GiB = 148.8 GB.\nvs the >=30 GB requirement: PASS by 5.0x.\nMageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.13 GB = 22.5 GiB fits on card 1 ALONE with 73.1 GiB spare — zero contention with the inference card, which is strictly better than the Tier B \"share both cards\" model.\n\nROBUSTNESS CHECK — the verdict does not depend on quantization at all. Unquantized BF16 weights + BF16 KV, TP=1: 51.75 + 4.0 + 1.18 + 16.0 = 72.00 GiB = util 0.75 on one card. Free = 23.6 + 95.6 = 119.2 GiB. STILL TIER A. There is no plausible config in which this model fails the 30 GB requirement.\n\nNet vs today: DeepSeek V4 Flash consumes 94.5 GB x 2 = 189 GB with ~2 GB free. This reclaims ~147 GB.",
        "vllm_ready": "YES — and uniquely well-evidenced, because it has been run on THIS EXACT hardware by a third party. github.com/theogravity/dual-rtx-6000-blackwell-qwen3.6-27b-fp8 is a config+benchmark repo for Qwen3.6-27B-FP8 on dual RTX PRO 6000 Blackwell (192 GB, no NVLink), CUDA capability 12.0. Separately, lastloop-ai/vllm-blackwell-guide runs it TP=1 on a single RTX PRO 6000 at --gpu-memory-utilization 0.92 — direct evidence for the Tier A single-card path.\n\nMANDATORY sm_120 flags (field-verified, not inferred):\n- --disable-custom-all-reduce : vLLM's custom all-reduce kernel does not support CC 12.0. (Moot at TP=1 — another reason to prefer TP=1.)\n- --mamba-ssm-cache-dtype float16 AND --mamba-cache-dtype float16 : \"mandatory for correctness\"; the DeltaNet state must NOT be fp8.\n- --kv-cache-dtype fp8_e4m3 requires the FULL system CUDA 13 toolkit (cuda-nvcc-13-0 etc.); PyPI CUDA fragments fail flashinfer JIT on sm_120.\n- MTP speculative decoding works at num_speculative_tokens<=3 (74.8% acceptance); spec=4 triggers EngineDeadError on CC 12.0.\n- Known issue: \"cuda graph capture size larger than mamba cache size\" — reduce --max-cudagraph-capture-size from the 512 default.\n\nREAL CAVEAT, and I want to flag it rather than wave it through: this is a NIGHTLY-container path, not a settled stable-release path. The model card asks for vllm>=0.19.0; the working dual-6000 config used vllm/vllm-openai:cu129-nightly, and official Qwen recipes say \"For Blackwell GPUs, use vllm/vllm-openai:cu130-nightly.\" For a production service whose owner already took an outage from one memory-utilization change, pinning a nightly container digest (not a floating :nightly tag) is a real operational requirement, not a nicety.\n\nTP=1 is both the VRAM-optimal AND the latency-optimal choice — the Blackwell guide explicitly warns against TP=2 for a 27B because \"NCCL all-reduce over PCIe adds more latency than it saves.\" These two goals happily coincide; the survey missed this.",
        "quality_vs_deepseek": "The survey's \"parity within noise\" claim DOES NOT HOLD UP, and its methodology repeats the warned-against error in a subtler form. I fetched Qwen's model card: its benchmark tables compare against Qwen3.5-27B, Qwen3.5-397B-A17B, Gemma4-31B, Claude 4.5 Opus and Qwen3.6-35B-A3B. There is NO DeepSeek column anywhere on the card. The survey's \"86.2 vs 86.4 / 87.8 vs 88.1 / 77.2 vs 79.0\" was assembled by reading two vendors' self-reported cards side by side — different harnesses, different prompting, different effort settings. That is not a head-to-head.\n\nActual neutral head-to-head evidence does exist, and it favors DeepSeek:\n- Artificial Analysis Intelligence Index (9 evals combined): DeepSeek V4 Flash (Reasoning, Max Effort) 40 vs Qwen3.6-27B (Reasoning) 37. A 3-point composite gap is a real regression, not noise.\n- llm-stats head-to-head: DeepSeek wins 7 (GPQA, HLE, IMO-AnswerBench, HMMT Feb 26, GDPval-AA, SWE-bench Verified, SWE-bench Multilingual); Qwen wins 2 (SWE-bench Pro, Terminal-Bench 2.0).\n\nTwo honest mitigations that matter for THIS product:\n1. The gap concentrates in agentic coding, frontier math and HLE — near-zero predictive value for \"read a 6.3k-token game state, pick from a numbered legal-action menu, justify in two sentences.\" The knowledge/reasoning benchmarks closest to that task are effectively tied (MMLU-Pro 86.2 vs 86.4, GPQA-Diamond 87.8 vs 88.1).\n2. The comparison is against DeepSeek at MAX EFFORT reasoning, but this product deliberately disables extended reasoning on the real-time path (commit 200ecff). The production-relevant gap is therefore narrower than 40 vs 37.\n\nBottom line: expect a small but real quality regression, concentrated in areas you do not exercise. Do not promote on benchmarks. This repo already owns the correct instrument — tools/eval/ exists precisely because public benchmarks do not predict mtgacoach quality. Capture >=30 live prompts and gate on the documented thresholds: legality >=4.5 (hard fail below), correctness/reasoning gap <0.5 vs the incumbent.\n\nSEPARATE AND MORE DANGEROUS THAN THE QUALITY GAP: Qwen3.6 is a reasoning model (--reasoning-parser qwen3, \"thinking preservation\" is a headline feature). BOTH hardware-matched sources went out of their way to disable thinking — theogravity ships \"a custom chat template with thinking mode off by default,\" lastloop \"injects enable_thinking: false for fast time-to-first-token.\" Artificial Analysis measures the hosted reasoning endpoint at TTFT 3.72s and 54.5 tok/s. If thinking is left on, output goes from ~60 tokens to hundreds, and you reproduce the exact 6.1s failure that already broke this product. Thinking-off must be enforced in the chat template, not merely requested per call.\n\nLatency outlook with thinking OFF (local, TP=1): 100 tok/s (INT4-AutoRound + MTP n=3 + FP8 KV), 115 tok/s (BF16 KV + flash_attn), 137 tok/s / 6.78 ms TPOT (FP8, TP=2 latency workload). A ~60-token answer is 0.44-0.60s decode; 6.3k-token prefill adds roughly 0.3-0.6s cold and much less warm with --enable-prefix-caching (reported 2.9-13.2x on multi-turn). Estimated ~0.75-1.2s cold, comfortably under warm. That STRADDLES the 0.9-1.1s budget rather than clearing it — treat as \"likely passes, must be measured,\" not confirmed. Ignore the 10,754 ms TTFT in the dual-card benchmark: that is a 32-concurrent saturated 175k-context throughput run, not your concurrency-1 profile.",
        "reasoning": "VERDICT: TIER_A confirmed, with a far larger margin than the survey claimed — but the survey reached the right answer through arithmetic that was wrong in four places, and its headline quality claim is not supported.\n\nWHAT SURVIVES VERIFICATION:\n- Release date 2026-04-21, confirmed from three official surfaces (HF API createdAt on both the base and FP8 repos, plus the QwenLM GitHub news entry). ~3.3 months old. Not stale, and not a listicle artifact.\n- Param count 27,781,427,952 all-BF16, and the config (64 layers, 24 Q heads, 4 KV heads, head_dim 256, hidden 5120, 262,144 native context) — all confirmed from the raw config.json and safetensors index.\n- Tier A single-card fit — confirmed, and it holds even with NO quantization at all (BF16 weights + BF16 KV = 72.00 GiB, util 0.75, one card). The verdict is robust to every quantization choice, which is a much stronger result than the survey's FP8-dependent argument.\n- vLLM on sm_120 — better evidenced than for any realistic alternative, because someone published a working config for literally dual RTX PRO 6000 Blackwell.\n\nWHAT I HAD TO CORRECT:\n1. KV cache math wrong by 4.1x. This is NOT a uniform transformer — config.json shows layer_types with full_attention_interval=4, and the model card confirms \"16 x (3 x Gated DeltaNet -> 1 x Gated Attention)\". Only 16 of 64 layers cache KV. The survey multiplied by 64. Wrong in the safe direction, but it means the survey did not actually understand the architecture it was recommending.\n2. FP8 weights understated by 3.07 GB (27.8 claimed vs 30.87 actual). The official FP8 repo keeps 3.08B params in BF16; the survey did naive params x 1 byte.\n3. The DeltaNet/Mamba state cache was omitted entirely — and it is the term that actually causes OOM in the field. I derived ~76 MiB/sequence (fp16, mandatory), which predicts 18.94 GiB at max-num-seqs 256; that independently and exactly explains a real reported \"Mamba state cache OOM\" on an RTX PRO 6000. Given this owner just took a service outage from a memory-utilization misjudgement, an omitted memory term is the single most consequential kind of error in this survey.\n4. \"Parity within noise\" with DeepSeek V4 Flash is unsupported. Qwen's card contains no DeepSeek column; the survey stitched two self-reported cards together. Neutral head-to-head (Artificial Analysis) shows 37 vs 40 on the composite index, and DeepSeek winning 7 of 9 benchmarks. A real, if modest, regression.\n\nWHAT THE SURVEY MISSED IN THE MODEL'S FAVOR:\n- Its own \"vision tower overhead\" caveat is solved by --language-model-only, documented on the model card, which skips the vision encoder and multimodal profiling and hands that memory to KV.\n- TP=1 is simultaneously the VRAM-optimal and the LATENCY-optimal configuration (the Blackwell guide warns TP=2 costs more in PCIe all-reduce than it saves for a 27B). Freeing the second card entirely is not a trade-off against speed — it is also the faster setup. That is the strongest single argument for this candidate.\n\nRECOMMENDED CONFIG: TP=1 on card 0, FP8 weights, --language-model-only, --max-model-len 131072, --max-num-seqs 16, --kv-cache-dtype fp8_e4m3, --mamba-cache-dtype float16, --mamba-ssm-cache-dtype float16, --enable-prefix-caching, MTP n<=3, --gpu-memory-utilization 0.55. Leaves 138.6 GiB free (5.0x the requirement) with card 1 wholly untouched for MageZero + QLoRA.\n\nTOP RISKS, ranked: (1) thinking mode left on reproduces the known 6.1s product failure — enforce enable_thinking:false in the chat template, not per-request; (2) nightly-container dependency — pin a digest; (3) modest quality regression — gate with tools/eval/ on legality >=4.5 and correctness gap <0.5 before promoting; (4) latency straddles rather than clears the 0.9-1.1s budget — measure concurrency-1 TTFT with prefix caching warm before committing. Recommend proceeding to a bake-off on card 1 while DeepSeek keeps serving, so the incumbent is never taken down to test the replacement."
      },
      {
        "name": "Qwen3.6-35B-A3B (Alibaba/Qwen)",
        "date_confirmed": "2026-04-15 (HF repo creation) / 2026-04-16 (official announcement) — CONFIRMED, survey agent's \"2026-04\" is correct. 3.5 months old today (2026-07-30). Not stale.",
        "date_source": "Two independent primary sources agree. (1) HuggingFace API https://huggingface.co/api/models/Qwen/Qwen3.6-35B-A3B returns createdAt=2026-04-15T05:59:19.000Z, lastModified=2026-04-24T02:53:42.000Z, author=Qwen, license Apache-2.0, safetensors BF16 total=35,951,822,704. (2) Official repo https://github.com/QwenLM/Qwen3.6 News: \"2026-04-16: Qwen3.6-35B-A3B is now availabe on Hugging Face Hub and ModelScope.\" (same page also lists \"2026-04-22: Qwen3.6-27B\", \"2026-02-24: Qwen3.5-122B-A10B, Qwen3.5-35B-A3B, Qwen3.6-27B\"). The FP8 sibling https://huggingface.co/api/models/Qwen/Qwen3.6-35B-A3B-FP8 also shows createdAt=2026-04-15. No listicle inference involved. This is NOT a repeat of the Llama-4-Scout error.",
        "stale": false,
        "fits_verdict": "TIER_A",
        "vram_math": "CARD = 97,887 MiB = 95.59 GiB = 102.64 GB. Pair = 191.19 GiB = 205.3 GB.\n\nWEIGHTS (from HF safetensors param count, not a blog round number):\n- Params = 35,951,822,704 (HF API, primary).\n- BF16 = 35.95e9 x 2 B = 71.90 GB = 66.96 GiB. Independently corroborated: Yotta Labs guide says \"BF16 weights requiring approximately 71GB\".\n- FP8 E4M3 (Qwen/Qwen3.6-35B-A3B-FP8, official) = 34.45 GB F8_E4M3 + 1.50 GB BF16 kept high-precision = 35.95 GB = 33.48 GiB. MEASURED loaded on an actual RTX PRO 6000 Blackwell 96GB: 34.3 GiB. My arithmetic and the measurement agree within 2.5%.\n- NVFP4 (nvidia/Qwen3.6-35B-A3B-NVFP4) exists but see vllm_ready — do not use it on sm_120.\n\nKV CACHE — the survey agent got the right number for the wrong reason, and I nearly \"corrected\" it wrongly:\n- Theoretical floor from config.json: 40 layers but full_attention_interval=4, so only 10 layers are full attention (indices 3,7,11,...,39 — corroborated independently by the FP8 checkpoint's quantization_config, which excludes attention modules at exactly those 10 indices). 10 layers x 2 kv_heads x 256 head_dim x 2 (K+V) = 10,240 values/token -> BF16 = 20.0 KiB/token. The other 30 layers are Gated DeltaNet linear attention with a per-SEQUENCE constant recurrent state, not per-token KV.\n- MEASURED effective rate on this exact GPU (RTX PRO 6000 Blackwell SM 12.0 96GB, vLLM 0.19.1rc1.dev322.cu130, official FP8 checkpoint): KV pool 21.4 GiB held 279,840 tokens = 82.1 KiB/token. vLLM's hybrid allocator pads attention pages up to the GDN/mamba block size, so the practical rate is ~4x the theoretical floor. PLAN WITH 82.1 KiB/token (BF16 KV), not 20.0. With --kv-cache-dtype fp8 it roughly halves to ~41 KiB/token (lastloop guide reports \"2x KV capacity\" for FP8 KV, consistent).\n- So the survey's \"82 KB/token BF16\" is the correct PLANNING number even though it was derived by wrongly counting all 40 layers as full-attention. Its downstream example is wrong the other way: \"8k/seq = 0.33 GB FP8\" — actual is 8,192 x 41 KiB = 0.34 GiB FP8 KV / 0.64 GiB BF16 KV. Close enough that the conclusion is unaffected.\n- 128k context (131,072 tokens), ONE sequence: BF16 KV = 131,072 x 82.1 KiB = 10.26 GiB (11.02 GB). FP8 KV = 5.13 GiB.\n- Real workload (6,300 tokens): 0.49 GiB BF16 KV per sequence.\n\nSINGLE-CARD BUDGET (FP8 weights, BF16 KV, --tensor-parallel-size 1, --gpu-memory-utilization 0.90):\n  0.90 x 95.59 GiB = 86.03 GiB budget\n  - 34.3 GiB weights (measured)\n  -  ~2.0 GiB runtime/CUDA graphs/activations\n  = 49.7 GiB KV pool = 49.7 GiB / 82.1 KiB = ~634,000 tokens\n  -> 4.8 concurrent FULL 128k sequences, or ~100 concurrent 6,300-token production prompts.\n  128k for a single coaching session costs 10.26 GiB of that 49.7 GiB. Trivially covered.\n\nHEADROOM vs THE >= 30 GB REQUIREMENT:\n- Even at the conservatively MEASURED 0.60-util config (55.7 GiB total on card 0, KV still 279,840 tokens = 44 concurrent 6.3k prompts): card 0 free = 95.59 - 55.7 = 39.9 GiB = 42.8 GB. Card 1 free = 95.59 GiB = 102.6 GB. TOTAL FREE = 135.5 GiB = 145.5 GB.\n- At 0.90 util on card 0: card 0 free ~9.6 GiB + card 1 free 102.6 GB = ~112.9 GB free.\n- Requirement is >= 30 GB. Worst case above clears it by 3.8x, typical case by 4.8x.\n- MageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.13 GB. That fits inside card 0's leftover ALONE at 0.60 util (42.8 GB), and card 1 stays 100% empty. Or run both trainers on card 1 with 78 GB to spare.\n- Delta vs today: DeepSeek V4 Flash uses ~160 GB of weights across the pair and 94.5 GB/card steady-state, leaving ~8 GB total. This swap frees ~135-145 GB.\n- BF16 fallback (if you refuse to quantize): 66.96 GiB weights + 2 GiB runtime on one card at util 0.95 (90.81 GiB) leaves 21.9 GiB KV = 268k tokens = 42 concurrent 6.3k prompts. Still TIER A (card 1 entirely free), but no reason to — FP8 is the officially published checkpoint and is what every sm_120 measurement used.",
        "vllm_ready": "YES on sm_120 today, but on FALLBACK kernels, and the measured throughput spread is the single biggest execution risk. Be precise about this:\n\nWHAT WORKS (measured on real RTX PRO 6000 Blackwell, SM 12.0, 96GB):\n- Official FP8 checkpoint serves on ONE card, TP=1: 208.6 tok/s decode, TTFT 25 ms, vLLM 0.19.1rc1.dev322+g03f8d3a54.cu130, driver 595.97/CUDA 13.0, 400W cap, flags --gpu-memory-utilization 0.60 --max-model-len 16384 --max-num-seqs 512. (Allen Kuo, Medium.) This also beat Ollama Q4_K_M (144.2 tok/s) by 45%.\n- lastloop-ai/vllm-blackwell-guide (repo verified to exist, title: \"Qwen3.6 27B/35B on RTX PRO 6000 Blackwell — 120-200 tok/s\"): 170 tok/s with CUDA graphs + flashinfer + MTP n=3 + FP8 KV on 1x RTX PRO 6000. Requires the FULL system CUDA 13 toolkit — \"flashinfer JIT fails with the PyPI CUDA 13 fragments on sm_120.\"\n- Model card: vLLM >= 0.19.0, SGLang >= 0.5.10.\n\nKNOWN sm_120 DEFECTS (do not discover these in production):\n- vLLM issue #44688 (opened 2026-06-05, OPEN, no maintainer fix): Qwen3.6-35B-A3B-FP8 on RTX PRO 6000 Blackwell Server Edition gets only ~72 tok/s single-GPU / 150-165 tok/s aggregate at TP=2, on vLLM 0.21.0 and 0.22.1. Root cause: no tuned MoE config for E=256,N=256 on this device (\"Using default MoE config. Performance might be sub-optimal!\"), and SM_120/121 has no CUTLASS path for tensor/token-scaled FP8 grouped GEMM, so it falls back to the TRITON Fp8 MoE backend.\n- That is a 2.9x spread (72 vs 208.6 tok/s) on the same GPU family and same checkpoint, driven purely by vLLM build + backend flags. It is a config problem, not a model problem, but it MUST be reproduced on blackwell before cutover.\n- NVFP4 is WORSE on sm_120, not better: \"The native CUTLASS NVFP4 MoE path is broken on SM120 due to TMA WS grouped GEMM initialization failures\" — only the Marlin W4A16 dequant fallback works. Do not choose NVFP4 here.\n- GDN footgun: default --max-num-seqs 1024 exceeds available mamba/GDN cache blocks (981 observed at 0.60 util) and the server fails. Set --max-num-seqs 512 or lower.\n- Caveat on transfer: both cited measurements are on 400W/600W Server/desktop parts; your cards are Max-Q (power-limited). Decode is memory-bandwidth-bound so tok/s should transfer well; prefill is compute-bound and will be somewhat slower.\n- Minor: this is a multimodal checkpoint (pipeline_tag image-text-to-text, arch Qwen3_5MoeForConditionalGeneration, vision tower included in the 35.95B). Text-only serving is fine; cap --limit-mm-per-prompt to avoid pointless mm preprocessing.\n\nLATENCY vs YOUR HARD BUDGET (0.9-1.1s good, 6.1s = product failure):\n- 6,300-token prompt, ~50-token answer, thinking OFF. Prefill 2 x 3e9 active x 6,300 = 3.8e13 FLOPs -> ~0.15-0.5s even on the Triton fallback. Decode 50 tokens at 170-208 tok/s = 0.24-0.29s; at the degraded 72 tok/s = 0.69s. Total ~0.4-0.85s good case, ~1.2s degraded case. Meets budget in the good config.\n- HARD REQUIREMENT: this model is thinking-by-default. Card: emits <think>...</think> unless you pass chat_template_kwargs {\"enable_thinking\": false}; recommended output length is 32,768 tokens (81,920 for hard problems). If thinking leaks onto the real-time path you reproduce the exact 6.1s failure that commit 200ecff fixed. Serve with --reasoning-parser qwen3 AND enable_thinking:false, and keep the existing CoT-to-TTS guard.\n- Non-thinking sampling params per the card: temperature=0.7, top_p=0.80, top_k=20, presence_penalty=1.5.",
        "quality_vs_deepseek": "NO head-to-head exists in any primary source. Do not assert parity. What is actually published:\n\nQwen3.6-35B-A3B model card (THINKING mode, Qwen's own eval): MMLU-Pro 85.2, GPQA 86.0, AIME26 92.7, LiveCodeBench v6 80.4, SWE-bench Verified 73.4, SWE-bench Pro 49.5, MMMU 81.7. Its comparison column is Qwen3.5-35B-A3B (85.3 / 84.2 / 91.0 / 74.6 / 70.0 / 44.6 / 81.4). NOTE: I could not confirm the survey agent's claim that the card also compares against Gemma4-31B, Gemma4-26B-A4B and Claude-Sonnet-4.5 — the table I retrieved is a Qwen3.5-vs-3.6 delta table. Treat that part of the survey report as unverified.\n\nDeepSeek-V4-Flash model card (284B total / 13B active, 1M ctx, MIT), which publishes per-effort columns:\n  MMLU-Pro      Non-Think 83.0 | High 86.4 | Max 86.2\n  GPQA-Diamond  Non-Think 71.2 | High 87.4 | Max 88.1\n  LiveCodeBench Non-Think 55.2 | High 88.4 | Max 91.6\n  SWE Verified  Non-Think 73.7 | High 78.6 | Max 79.0\nThe survey agent's \"86.4 / 88.1 / — / 79.0\" is the MAX-REASONING column. Correctly sourced, but it is the wrong column for your product.\n\nTHE THING THAT ACTUALLY MATTERS: your real-time path runs with extended reasoning DISABLED (commit 200ecff). So the honest comparison is non-think vs non-think. DeepSeek V4 Flash in non-think loses 16.9 GPQA points, 3.4 MMLU-Pro, 36.4 LiveCodeBench, 5.3 SWE-V versus its own max mode. Qwen publishes NO non-thinking benchmark table at all. Therefore the production-mode quality delta between these two models is UNMEASURED in public data, in both directions — it is entirely possible Qwen3.6-35B-A3B non-think beats DeepSeek V4 Flash non-think, and equally possible it does not.\n\nComparing the published numbers apples-to-apples at max effort, DeepSeek leads by 1.2 (MMLU-Pro), 2.1 (GPQA-D), 5.6 (SWE-V), 11.2 (LCB). Even taken at face value those are cross-card numbers from two different vendors' harnesses and should not be treated as a controlled comparison.\n\nFor your actual task — read ~6k tokens of structured game state, pick from a numbered legal-action menu, explain in two sentences — none of these benchmarks are load-bearing. SWE-bench and LiveCodeBench are irrelevant; the closest proxies are MMLU-Pro/GPQA, where the max-effort gap is 1-2 points. The decisive evidence has to come from tools/eval/ on your own captured prompts, scoring correctness/reasoning/conciseness/LEGALITY. Legality < 4.5 disqualifies it from the autopilot path regardless of everything above.",
        "reasoning": "VERDICT: TIER_A, high confidence. The survey agent's headline conclusion is correct and survives adversarial checking; two of its supporting details do not.\n\n1) RELEASE DATE — CONFIRMED, not inferred from a listicle. HF API createdAt 2026-04-15 and the QwenLM/Qwen3.6 GitHub news line dated 2026-04-16 agree. 3.5 months old. Passes the recency bar cleanly; this is not another Llama-4-Scout.\n\n2) VRAM — PASSES BY A LARGE MARGIN, and I verified it against a real measurement on your exact GPU rather than trusting arithmetic alone. Official FP8 checkpoint = 33.48 GiB by params x bytes, measured at 34.3 GiB loaded on an RTX PRO 6000 Blackwell 96GB. It fits ONE card with a 49.7 GiB KV pool at 0.90 util (~634k tokens = 4.8 concurrent 128k sessions, ~100 concurrent 6.3k production prompts), freeing the entire second card. Even at the conservative measured 0.60-util config, total free across the pair is ~145 GB against a >= 30 GB requirement — 4.8x margin, and MageZero (12.53 GB) + QLoRA (11.6 GB) fit in card 0's leftover alone with card 1 still completely empty. This is a genuine Tier A, not a marginal one.\n\n3) WHERE THE SURVEY AGENT WAS WRONG (both immaterial to the verdict, one worth knowing):\n   a. Its KV derivation counted all 40 layers as full-attention. Only 10 are (full_attention_interval=4; corroborated by the FP8 checkpoint excluding attention at exactly layers 3,7,...,39 from quantization). The theoretical floor is 20.0 KiB/token, not 82. HOWEVER — and this is why I checked rather than \"corrected\" — vLLM's hybrid allocator pads attention pages to the GDN block size, and the measured effective rate on real hardware is 82.1 KiB/token, matching the survey's number by coincidence. Plan with 82.1. Its per-sequence example (0.33 GB at 8k FP8) is off in the opposite direction but harmlessly.\n   b. It omitted the GDN/mamba state pool entirely. That omission is what causes the actual, documented startup failure: default --max-num-seqs 1024 exceeds the available 981 mamba cache blocks. Set --max-num-seqs 512.\n   c. Its claim that the model card benchmarks against Gemma4-31B / Gemma4-26B-A4B / Claude-Sonnet-4.5 did not reproduce — the table I retrieved compares only against Qwen3.5-35B-A3B. Treat that claim as unverified.\n\n4) vLLM ON sm_120 — WORKS TODAY, ON FALLBACK KERNELS, WITH A 2.9x THROUGHPUT SPREAD. This is the real risk, and it is a deployment risk, not a model risk. SM_120 has no CUTLASS FP8 grouped-GEMM path (Triton fallback) and no tuned MoE config for E=256,N=256 on RTX PRO 6000; vLLM issue #44688 (open since 2026-06-05, no maintainer fix) reports 72 tok/s because of it, while a correctly-configured single card with flashinfer + CUDA graphs + FP8 KV + MTP n=3 measures 170-208.6 tok/s. NVFP4 is not the escape hatch — the CUTLASS NVFP4 MoE path is outright broken on SM120 and only Marlin W4A16 dequant works. Requires the full system CUDA 13 toolkit for flashinfer JIT.\n\n5) LATENCY — MEETS THE BUDGET, CONDITIONAL ON ONE FLAG. At 170-208 tok/s decode and a ~6.3k-token prefill on 3B active params, expect ~0.4-0.85s end to end, inside your 0.9-1.1s target, with margin the current DeepSeek deployment does not have. But this model is THINKING-BY-DEFAULT with a recommended 32,768-token output length. Ship it without chat_template_kwargs {\"enable_thinking\": false} and --reasoning-parser qwen3 and you will reproduce the 6.1s product failure exactly.\n\n6) QUALITY — THE ONE THING I CANNOT SETTLE FROM PUBLIC DATA, AND NEITHER COULD THE SURVEY AGENT. There is no head-to-head. At max reasoning effort DeepSeek V4 Flash leads by 1.2 MMLU-Pro / 2.1 GPQA-D / 5.6 SWE-V, cross-card across two vendors' harnesses. More importantly, your production path runs non-think, DeepSeek's non-think scores collapse (GPQA-D 88.1 -> 71.2), and Qwen publishes no non-think table at all — so the delta in the mode you actually ship is unmeasured in both directions. The survey agent's \"the gap is likely immaterial\" is a reasonable guess but it is a guess.\n\nRECOMMENDED NEXT STEP: this is worth a real trial, not a blind cutover. Serve Qwen3.6-35B-A3B-FP8 on GPU 1 alone (TP=1, --gpu-memory-utilization 0.60, --max-model-len 262144, --max-num-seqs 512, --kv-cache-dtype fp8, flashinfer, enable_thinking:false) while DeepSeek keeps running, register it as a second LiteLLM model, and A/B it through tools/eval/ on captured production prompts. The gating metric is legality >= 4.5 and correctness/reasoning within 0.5 of the incumbent — not MMLU-Pro. Confirm decode lands near 170-208 tok/s and not 72 before committing; if it lands at 72, that is issue #44688 and the fix is backend flags plus a tuned E=256,N=256 MoE config, not a different model."
      },
      {
        "name": "Gemma 4 31B (dense) and Gemma 4 26B-A4B (MoE), Google DeepMind",
        "date_confirmed": "2026-03-31 (models), 2026-04-02 (launch blog). CONFIRMED — survey agent was correct. ~4 months old, well inside the recency bar; not stale.",
        "date_source": "PRIMARY: ai.google.dev/gemma/docs/releases — \"March 31, 2026: Release of Gemma 4 in E2B, E4B, 31B and 26B A4B sizes\". Launch blog 2026-04-02. HuggingFace API createdAt fetched via curl: gemma-4-31B-it = 2026-03-11T18:22:36Z, gemma-4-26B-A4B-it = 2026-03-11T21:25:57Z (pre-launch staging). Tech report arXiv 2607.02770. Same log confirms gemma-4-12B Unified is a separate 2026-06-03 release and adds an MTP drop 2026-04-16. No listicle used for dating.",
        "fits_verdict": "TIER_A",
        "vram_math": "Card: 95.59 GiB each (97,887 MiB); 0.95 util = 90.81 GiB usable.\n\nWEIGHTS (measured from HF safetensors blob bytes, not blog rounding):\n  31B BF16 = 58.25 GiB | 31B NVFP4 = 30.39 GiB | 31B FP8-block = 30.98 GiB\n  26B BF16 = 48.07 GiB | 26B NVFP4 = 17.50 GiB | 26B FP8-dyn   = 26.67 GiB\n  Cross-check: 31,273,088,876 BF16 params x 2B = 62.546 GB vs 62.546 GB on disk. Confirmed.\n\nKV @128k (SURVEY AGENT OFF BY ~12x — missed 1024-token sliding cap on 50/60 layers AND the separate global head shape num_global_key_value_heads=4 x global_head_dim=512):\n  31B: full 10 x 2 x 4 x 512 x 2B = 81,920 B/tok x 131,072 = 10.00 GiB\n     + slide 50 x 2 x 16 x 256 x 2B x 1024 (capped)        =  0.78 GiB  => 10.78 GiB\n  26B: full  5 x 2 x 2 x 512 x 2B = 20,480 B/tok x 131,072 =  2.50 GiB\n     + slide 25 x 2 x 8 x 256 x 2B x 1024                  =  0.20 GiB  =>  2.70 GiB\n  (Survey's 983/246 KB per token = the pre-vLLM-0.21 bug, confirmed by vLLM #43308: 55k->525k tokens, 9.5x.)\n\nFIT, single card, TP=1 (weights + KV vs 90.81 GiB):\n  26B NVFP4  17.50+2.70 = 20.20 -> 70.61 spare + 95.59 free card = 166.21 GiB free\n  26B FP8    26.67+2.70 = 29.37 -> 61.44 spare + free card       = 157.04 GiB free\n  26B BF16   48.07+2.70 = 50.77 -> 40.04 spare + free card       = 135.64 GiB free\n  31B NVFP4  30.39+10.78= 41.17 -> 49.64 spare + free card       = 145.24 GiB free\n  31B BF16   58.25+10.78= 69.03 -> 21.78 spare + free card       = 117.38 GiB free\nAll six are TIER A: one card entirely, second card 100% free.\n\nREQUIREMENT: >=30 GB (27.94 GiB) free. Need MageZero 12.53 GB + QLoRA 11.6 GB = 24.13 GB = 22.47 GiB. The freed card alone gives 95.59 GiB — exceeded 4-7x, both jobs fit on the free card with ~71 GiB spare. (Today's DeepSeek V4 Flash: 94.5 GB/card, ~2 GB free.)",
        "vllm_ready": "YES on sm_120, with one hard version floor. Architecture is Gemma4ForConditionalGeneration (vision+audio wrapper); vLLM's supported-models table lists only the text-only Gemma4ForCausalLM entry, but that is doc incompleteness, not a gap — RedHatAI (the llm-compressor/vLLM team) publishes an explicit `vllm serve` command for gemma-4-26B-A4B-it-FP8-dynamic with `--reasoning-parser gemma4 --tool-call-parser gemma4`, and live vLLM issues (#39133, #43308) show people serving gemma-4-31B today.\n\nBlackwell evidence is first-party: NVIDIA ships nvidia/Gemma-4-31B-IT-NVFP4 (2.8M downloads) and nvidia/Gemma-4-26B-A4B-NVFP4 (1.2M), naming \"NVIDIA Blackwell\" as the target microarchitecture with vLLM as the runtime. NVFP4 is the Blackwell-native 4-bit format (sm_120/sm_100). RedHatAI FP8 variants have 5.3M/1.35M downloads. Independent sm_120 recipe exists for the gemma4 family (lna-lab/gemma4-12b-vllm-sm120, 50-118 tok/s on no-NVLink SM120).\n\nHARD REQUIREMENT: vLLM >= 0.21.0. v0.21.0 fixed the hybrid sliding-window + shared-KV allocator for Gemma 4 (issue #43308: KV capacity 55k -> 525k tokens, ~9.5x, memory pool unchanged). On v0.20.0 you get the survey agent's bad KV number for real and the fit degrades badly. Verify the gateway's vLLM version before cutover.\n\nNOT a blocker: vLLM #41403 (\"TurboQuant + Gemma 4, 5-gate blocker stack\", all 5 gates open as of 2026-04-30) is scoped to TurboQuant 4-bit KV *compression* on sm_120, motivated by fitting native 262k context on 4x16GB consumer Blackwell. Plain BF16/FP8/NVFP4 serving faces no documented blocker, and this deployment never needs it — BF16 KV at 128k costs only 2.70-10.78 GiB.\n\nQuantization is low-risk: RedHatAI FP8 reports 99.3-102.1% accuracy recovery vs BF16; NVIDIA NVFP4 reports ~0.5-0.7% average degradation (MMLU Pro 85.00 -> 84.80, GPQA 80.30 -> 79.90).",
        "quality_vs_deepseek": "Head-to-head DOES exist (survey agent wrongly said none). AA Intelligence Index: DeepSeek V4 Flash 40 vs Gemma 4 31B 29 vs 26B-A4B 26 — real composite gap, no parity claim. But near-parity on the closest task proxy: MMLU-Pro 86.2 vs 85.2 (31B) / 82.6-85.0 (26B). Gaps concentrate in coding (LiveCodeBench 91.6 vs 80.0), agentic (SWE-bench 79.0 vs n/a) and frontier reasoning (HLE 34.8 vs 19.5) — none of which resemble \"pick a legal action from a numbered menu and explain in 2 sentences.\" DeepSeek V4 Flash is 284B-A13B, a much bigger model. True regression on this domain is unmeasured; validate with the repo's own tools/eval/ harness (legality>=4.5 gate) before cutover.",
        "stale": false,
        "reasoning": "VERDICT: TIER A, confirmed — but the survey agent reached the right answer through arithmetic that was wrong by ~12x, and it missed the fact that actually decides which variant to deploy.\n\nWHAT I CONFIRMED: release date (primary source, ~4 months old), exact parameter counts (32,682,372,656 and 26,544,131,376 — matched to the byte), Apache 2.0 license (HF cardData), 262,144 context (config.json), and the gemma-4-12B-is-a-separate-June-release caveat. Good work by the survey agent on all of those.\n\nWHAT I CORRECTED:\n1. KV cache — the survey agent said \"KV is the problem\" and quoted 983 KB/token. The real config.json shows 50 of 60 layers are sliding-window capped at 1024 tokens, and the 10 full-attention layers use num_global_key_value_heads=4 x global_head_dim=512, not 16 x 256. Real cost at 128k is 10.78 GiB (31B) and 2.70 GiB (26B) per sequence, not ~120 GiB. Their figure is precisely the pre-vLLM-0.21 pathological allocation. KV is a NON-ISSUE for this model; their central technical concern was backwards. The error was conservative, so the Tier A verdict survived by luck.\n2. \"Both are single-card at FP8\" understates it — the 26B-A4B fits one card at native BF16 (50.77 GiB) with zero quantization risk, and even the 31B dense fits at BF16 (69.03 GiB).\n3. \"No head-to-head published\" is false; AA and DocsBot both publish one, and it shows a real composite gap (40 vs 29/26) the survey agent did not surface.\n\nTHE DECISIVE FACT THE SURVEY AGENT MISSED — LATENCY. The owner's budget is hard (0.9-1.1s good, 6.1s = product failure), and the two variants are NOT interchangeable on it. At 1,792 GB/s per card:\n  - 31B DENSE BF16: 62.55 GB read/decode-step = 28.6 tok/s ceiling. Artificial Analysis independently measures 35.1 tok/s. A 40-60 token answer = 1.4-2.1s of decode alone, plus TTFT (AA: 1.10s). Realistic 1.8-2.8s. That MISSES the budget by 2-3x. At NVFP4 it improves to ~55 tok/s (~1.1s decode) — merely borderline.\n  - 26B-A4B MoE: only ~4.6B params are read per token (1.43B from 8/128 experts + 0.18B shared + ~2.96B attention/embed of the 22.84B expert pool). BF16 active read ~9.1 GB = ~196 tok/s ceiling; NVFP4 ~3.3 GB = ~537 tok/s. A 60-token answer costs ~0.3s (BF16) or ~0.11s (NVFP4). COMFORTABLY inside budget — and it has 3.8B active vs DeepSeek V4 Flash's 13B, so it should be FASTER than what is running today (AA measures DSV4F at 122 tok/s).\n\nRECOMMENDATION: deploy gemma-4-26B-A4B, not the 31B dense. NVFP4 (nvidia/Gemma-4-26B-A4B-NVFP4, 17.50 GiB) or FP8-dynamic (RedHatAI, 26.67 GiB) on ONE card, TP=1, --max-model-len 131072. That leaves the entire second card (95.59 GiB) plus ~61-71 GiB on the serving card — roughly 157-166 GiB free against a 24.13 GB requirement. The 31B dense is VRAM-fine but latency-marginal and is the wrong trade for a real-time voice product; its only edge is +3 points of AA index, on evals that do not resemble this task.\n\nCAVEATS TO CARRY FORWARD: (a) confirm the gateway runs vLLM >= 0.21.0 or the KV math collapses to the survey agent's bad number; (b) the quality regression vs DeepSeek V4 Flash is real on composite benchmarks and UNMEASURED on this domain — run tools/eval/ against >=30 captured live prompts before cutover, applying the repo's own legality>=4.5 gate; (c) NVIDIA's NVFP4 checkpoint is TP=1 only, which suits Tier A but forecloses scaling to TP=2 on that specific artifact."
      },
      {
        "name": "DiffusionGemma 26B-A4B-it (Google DeepMind)",
        "date_confirmed": "2026-06-09 — CONFIRMED, survey agent was right. Triangulated across three independent sources: (1) HF API `createdAt` = 2026-06-09T12:40:12Z on the official `google` org repo `google/diffusiongemma-26B-A4B-it`; (2) NVIDIA's official blog post announcing it, published 2026-06-10; (3) ai.google.dev/gemma/docs/diffusiongemma/model_card footer \"Last updated 2026-06-10 UTC\". ~7 weeks old today. NOT stale — this is one of the genuinely recent candidates, and unlike the Llama-4-Scout trap the date came from repo/blog primaries, not a listicle. Correction to the survey agent on one point: it said the model is \"not listed on ai.google.dev\" and held confidence at medium for that reason — in fact there IS a dedicated official model card at ai.google.dev/gemma/docs/diffusiongemma/model_card. Confidence should be HIGH, not medium.",
        "date_source": "Primary: HuggingFace API https://huggingface.co/api/models/google/diffusiongemma-26B-A4B-it → createdAt=2026-06-09T12:40:12.000Z, lastModified=2026-07-15, 1.93M downloads, config_architectures=[\"DiffusionGemmaForBlockDiffusion\"], safetensors total=25,823,778,864. Corroborating primaries: official Google model card https://ai.google.dev/gemma/docs/diffusiongemma/model_card (updated 2026-06-10) and NVIDIA official blog https://blogs.nvidia.com/blog/rtx-ai-garage-local-gemma-diffusion/ (published 2026-06-10).",
        "fits_verdict": "TIER_A",
        "vram_math": "All figures BF16 (no quantization needed, and none usable — see vllm_ready). Card = 97,887 MiB = 95.59 GiB. Pair = 191.19 GiB.\n\nWEIGHTS, checked against params x bytes-per-param rather than a blog round number:\n  25,823,778,864 params x 2 B (BF16) = 51,647,557,728 B = 48.10 GiB (51.65 GB decimal).\n  Independent corroboration: Unsloth's deployment doc states \"52 GB\" for BF16/FP16. Matches to <1%.\n  (Survey agent's \"FP8 ~25.8 GB\" is an unsupported extrapolation — Google ships no FP8 checkpoint and the vLLM recipe is BF16-only. It is also unnecessary; see below.)\n\nKV CACHE @ 128k, from config.json, not estimated:\n  num_hidden_layers=30, layer_types = 25 sliding_attention + 5 full_attention (5:1 pattern),\n  num_key_value_heads=8, head_dim=256, sliding_window=1024.\n  Per token per layer = 2 (K+V) x 8 x 256 x 2 B = 8,192 B = 8 KiB.\n  5 full layers  x 131,072 tok x 8 KiB = 5,368,709,120 B = 5.00 GiB\n  25 sliding layers, capped at the 1,024-token window, NOT context length:\n                   25 x 1,024 x 8 KiB   =   209,715,200 B = 0.195 GiB\n  KV @128k total = 5.20 GiB.\n  The 5:1 sliding ratio is why context length is nearly free here. At the REAL 6.3k-token\n  workload KV is 0.31 + 0.195 = 0.51 GiB. At full 256k it is only 10.2 GiB.\n\nDIFFUSION-SPECIFIC BUFFER (the survey agent missed this entirely):\n  vLLM pre-allocates self_conditioning_probs = max_num_seqs x canvas_length x vocab_size.\n  4 x 256 x 262,144 = 268,435,456 elements = 0.5 GiB (BF16) / 1.0 GiB (FP32).\n  This scales LINEARLY with max_num_seqs and the recipe caps it at 4 for that reason —\n  at max_num_seqs=32 it would be 4-8 GiB. Hard concurrency ceiling, irrelevant for a\n  single-user coaching product but a real constraint.\n\nTIER A — ONE CARD, TP=1:\n  weights 48.10 + KV@128k 5.20 + diffusion buffer 1.0 + activations/CUDA graphs/frag ~4.0\n  = 58.3 GiB on card 0.\n  Card 0 free  = 95.59 - 58.3 = 37.3 GiB\n  Card 1 free  = 95.59 GiB (entirely released)\n  TOTAL FREE   = 132.9 GiB  vs the >= 30 GB requirement. Met ~4.4x over.\n  MageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.13 GB (22.5 GiB) fits on card 1\n  alone, leaving 73 GiB spare there AND 37 GiB spare on card 0.\n\nContrast with the incumbent: DeepSeek V4-Flash is 79.7 GB weights/card x2 and 94.5 GB\nsteady-state/card — effectively the entire pair. Set --gpu-memory-utilization 0.65\n(62.1 GiB) with --tensor-parallel-size 1. Critically, unlike DeepSeek there is NO\nOOM-on-load risk at reduced utilization: weights are 48.10 GiB = 50.3% of one card, so\n0.65 leaves 14 GiB of slack over the weight load. 0.95 is not a floor for this model.\nVRAM is the strongest part of this candidate and it is not close.",
        "vllm_ready": "PROBABLE BUT UNPROVEN ON sm_120 — the survey agent's \"verify before committing\" was the right instinct and I could not close it.\n\nSupported in principle: vLLM shipped day-zero DiffusionGemma support, v0.24.0+ required, and there is an official vLLM recipe page. BUT that recipe validates \"Single GPU (H100/H200, BF16)\" ONLY. I found no published DiffusionGemma-on-sm_120 result anywhere.\n\nThree concrete sm_120 landmines, two of which definitely apply:\n1. APPLIES — head_dim=256 (confirmed in config.json) breaks the FlashInfer backend on\n   SM120: vLLM issue #40677, \"[Bug]: Gemma-4 fails when forcing FLASHINFER attention\n   backend on Blackwell SM120 (head_size not supported)\". Documented workaround: TRITON_ATTN\n   loads and runs correctly. So set VLLM_ATTENTION_BACKEND=TRITON_ATTN. Triton attention is\n   slower than the FlashInfer path Google/NVIDIA benchmarked — a latency tax, not a blocker.\n2. APPLIES — NVFP4/MXFP4 are unusable on sm_120: vLLM issue #31085 (open since 2025-12-20,\n   no fix PR linked) and flashinfer issue #2577 (\"NVFP4 mm_fp4 GEMM broken on SM120 — all\n   backends fail\"). NVIDIA's RTX blog specifically touts NVFP4 for DiffusionGemma; that path\n   is closed on these cards. BF16 only. Harmless here because BF16 already fits one card.\n3. UNKNOWN, the real risk — this is an MoE (128 experts, top-8) and CUTLASS MoE GEMM kernels\n   compiled for SM100 are reported not to initialize on SM120. BF16 MoE should route to\n   Triton/Marlin fallbacks that demonstrably work (Gemma 4 31B runs on dual RTX PRO 6000),\n   but the block-diffusion bidirectional-canvas attention path is a brand-new code path that\n   nobody has exercised on sm_120.\n\nCheap decisive test, ~1 hour, no risk to the running service: boot on card 1 only with\nCUDA_VISIBLE_DEVICES=1, --tensor-parallel-size 1, --gpu-memory-utilization 0.65,\n--max-model-len 131072, --max-num-seqs 4, VLLM_ATTENTION_BACKEND=TRITON_ATTN. DeepSeek keeps\nserving off the pair meanwhile. If it boots, gate it on tools/eval with captured real prompts.",
        "quality_vs_deepseek": "SUBSTANTIALLY WORSE, and this is measured, not inferred. The survey agent called quality \"the open question\" — that is too generous. It is not open; it is documented, by Google, and it is negative.\n\nGoogle's own model card publishes a same-harness head-to-head against the autoregressive parent it was distilled from, Gemma 4 26B-A4B. The diffusion variant loses on EVERY axis:\n  MMLU Pro         77.6 vs 82.6   (-5.0)\n  GPQA Diamond     73.2 vs 82.3   (-9.1)\n  AIME 2026        69.1 vs 88.3   (-19.2)\n  LiveCodeBench v6 69.1 vs 77.1   (-8.0)\n  Codeforces ELO   1429 vs 1718   (-289)\n  Tau2 (agentic)   56.2 vs 68.2   (-12.0)\n  MMMU Pro Vision  54.3 vs 73.8   (-19.5)\nThis is the intrinsic cost of discrete-diffusion sampling, published by the vendor with every\nincentive to minimize it. It is the cleanest evidence available and it is a primary source.\n\nAgainst the incumbent DeepSeek V4-Flash (284B total / 13B active, released 2026-04-24,\nvendor-reported): MMLU Pro 86.4 and GPQA Diamond 88.1. DiffusionGemma is -8.8 and -14.9.\nCross-vendor numbers are not same-harness, so treat magnitudes loosely — but combined with\nthe internal comparison the direction is unambiguous. No direct DiffusionGemma-vs-DeepSeek\nhead-to-head exists; I am not asserting one.\n\nThe number that matters most for THIS product is Tau2: 56.2 vs 68.2. Tau2 measures agentic\ntool-use and instruction-following under structured constraints — the closest public proxy\nfor \"read a numbered menu of legal actions, pick one, obey the output format.\" A 12-point\ndrop there hits exactly the failure mode the project already gates on (\"legality < 4.5 = can't\nbe the autopilot path\"). Selecting an action that isn't in the menu is a hard product failure,\nnot a soft quality regression.",
        "stale": false,
        "reasoning": "VERDICT: TIER_A on fit — decisively — but I would not ship it as a DeepSeek replacement without running the project's own eval harness first, and I expect that harness to fail it.\n\nThe tier question is purely about VRAM, and DiffusionGemma wins it outright. At plain BF16 it occupies ~58.3 GiB of one card, leaving 37.3 GiB free there plus the entire second card: ~133 GiB total headroom against a 30 GB requirement. MageZero (12.53 GB) and the gemma-4-12B QLoRA (11.6 GB) both fit on the freed card with 73 GiB to spare. No quantization required, no OOM-on-load fragility, and --gpu-memory-utilization can drop to 0.65 safely because weights are only half a card — the exact opposite of DeepSeek's 0.95 floor. Context is a non-issue: the 5:1 sliding-window pattern (25 sliding @1024 + 5 full) means 128k costs 5.20 GiB of KV and the real 6.3k-token workload costs 0.51 GiB.\n\nThree corrections to the survey agent. (1) Its confidence should be HIGH not medium — there IS an official ai.google.dev model card, it just didn't find it; the 2026-06-09 date is solid across three primaries. (2) Its FP8 figure is unsupported — no FP8 checkpoint ships, the vLLM recipe is BF16-only, and NVFP4 is outright broken on sm_120. Irrelevant, since BF16 already achieves Tier A. (3) It missed the self_conditioning_probs pre-allocation (max_num_seqs x 256 canvas x 262k vocab), which forces max_num_seqs=4 — fine for one user, a hard ceiling otherwise.\n\nI also want to deflate the latency case, which is the whole reason this candidate is interesting. The headline \">1100 tok/s\" is H100, FP8, low batch, and it rewards LONG generations. This product emits 1-2 sentences, ~40-60 tokens — a single 256-token canvas block. With entropy-bounded denoising at up to 48 steps, that is up to 48 forward passes over 256 positions, versus ~60 sequential forward passes for an autoregressive model. The real speedup comes from 3.8B active params vs DeepSeek's 13B, and from early termination when the sampler is confident — NOT from a 20x token-rate multiplier. On top of that, this hardware cannot use FlashInfer (head_dim 256, issue #40677) and cannot use NVFP4, so it runs the slower TRITON_ATTN BF16 path that nobody benchmarked. Expect comfortably inside the 0.9-1.1s budget and probably better than DeepSeek — but do not budget for \"effectively instant,\" and note that the obvious latency dial (--diffusion-eb-max-steps below 48) trades away quality this model cannot afford to lose. One genuine plus: thinking mode is OFF by default and opt-in via a <|think|> token, which aligns with the existing \"no extended reasoning on the real-time path\" fix in 200ecff.\n\nThe blocker is quality, and it is not speculative. Google's own table shows the diffusion sampling tax costs 5-19 points against the very model it was distilled from, including -12.0 on Tau2, the benchmark closest to structured menu-selection and format compliance. Against DeepSeek V4-Flash the gap is roughly -9 MMLU Pro and -15 GPQA. For a product whose failure mode is naming an action that isn't legal, that is the wrong direction on the wrong axis.\n\nRECOMMENDATION: worth a ~1-hour spike, not a commitment, and cheap to run — boot it on card 1 alone (CUDA_VISIBLE_DEVICES=1, TP=1, util 0.65, max-model-len 131072, max-num-seqs 4, VLLM_ATTENTION_BACKEND=TRITON_ATTN) while DeepSeek keeps serving off the pair. That answers the one thing I could not verify from sources: whether the block-diffusion MoE path initializes on sm_120 at all. If it boots, judge it with tools/eval on captured production prompts and let the legality dimension decide — not MMLU, and not the tok/s headline. My prior is that it fails the legality gate. If VRAM headroom is the binding constraint, the more promising direction is the autoregressive parent Gemma 4 26B-A4B, which scores 5-19 points higher on every published benchmark, has identical weight size at BF16, hence identical Tier A headroom, and runs a battle-tested sm_120 code path instead of a seven-week-old one."
      },
      {
        "name": "NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B (deploy the -FP8 build, NOT -NVFP4)",
        "date_confirmed": "2026-07-06 — CONFIRMED, 24 days old. Survey's \"2026-07\" is correct.",
        "date_source": "PRIMARY: model card README.md line 98-99 verbatim: \"### Release Date: July 6, 2026 via Hugging Face\". Corroborated by HF API createdAt: FP8 repo = 2026-06-24T15:33:18Z, NVFP4 repo = 2026-06-24T15:31:37Z, both lastModified 2026-07-07T10:58Z (repos staged 12 days before public release — normal). Survey's claim of three precision repos is right but the naming is off: nvidia/NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B-BF16 returns HTTP 200; the unsuffixed name returns 401 (does not exist publicly). The survey's family-vs-member caveat is valid and important: the Nemotron 3 FAMILY press release is 2025-12-15, but this Puzzle-compressed member is a July 2026 artifact. One nuance the survey missed: card line 368 states this model \"is initialized from NVIDIA-Nemotron-3-Super-120B-A12B and therefore inherits the parent model's pre-training and post-training data exposure\" — so the CHECKPOINT is 24 days old but the underlying knowledge is Dec-2025-vintage. Passes the recency bar on any reasonable reading; no stale-listicle error here.",
        "stale": false,
        "fits_verdict": "TIER_A",
        "vram_math": "SURVEY'S VRAM NUMBERS ARE WRONG — both quants, both optimistic. Verdict survives anyway.\n\nMEASURED WEIGHTS (sum of actual *.safetensors byte sizes from HF tree API, not estimated):\n- FP8 build: 83,034,621,632 B = 83.03 GB = 77.33 GiB. Survey said \"~75 GB\" — understated by 8 GB.\n- NVFP4 build: 53,503,824,640 B = 53.50 GB = 49.83 GiB. Survey said \"44.5 GB\" — understated by 9 GB (20% error).\n\nSURVEY'S ERROR MECHANISM (worth naming, it will recur): it read HF's safetensors.total field (44,539,891,200 for NVFP4) as a BYTE count. It is a PARAMETER count. It also ignored that 4.72B params never get quantized (embeddings — tie_word_embeddings=false so 2x 131072x4096 — plus norms, router, Mamba conv/A/D/dt, MTP head) and cost 2 bytes each.\nCross-check FP8 from dtype counts: F8_E4M3 73,572,286,464x1B + BF16 4,723,362,816x2B + F32 20,992x4B = 83.02 GB, matches file sum. Also note 75.3B \"total\" is marketing; real tensor count is 78.30B (includes the MTP speculative head).\n\nKV CACHE (computed from config.json — this is the model's killer feature and the survey understated it):\n88 layers = 40 mamba + 40 moe + only 8 ATTENTION. num_key_value_heads=2, head_dim=128.\nPer token = 2(K+V) x 2 heads x 128 dim x 8 layers = 4,096 elements.\n- FP8 KV: 4,096 B/tok -> 128k = 0.50 GiB | 256k = 1.00 GiB | 1M = 4.00 GiB\n- BF16 KV: 8,192 B/tok -> 128k = 1.00 GiB | 256k = 2.00 GiB | 1M = 8.00 GiB\nMamba state is per-SEQUENCE not per-token (survey omitted this entirely): conv 116,736 B + ssm 3,145,728 B (fp32, mamba_ssm_cache_dtype) per mamba layer x 40 = 130.3 MB/seq = 0.122 GiB/seq; at 8 concurrent = 0.98 GiB. Negligible for a single-user voice coach.\nFor scale: incumbent DeepSeek V4 Flash burns ~14.8 GB KV PER CARD (29.6 GB total, measured). Nemotron at 128k = 0.50 GiB. ~30x reduction.\n\nTIER A ARITHMETIC, TP=1 on ONE 95.6 GiB card:\n  weights 77.33 + KV@128k(FP8) 0.50 + mamba@8seq 0.98 + vLLM overhead ~4.0 = 82.8 GiB used\n  -> requires --gpu-memory-utilization ~0.87 (hard floor is 77.33/95.6 = 0.81 just to hold weights)\n  Card 0 free: 95.6 - 82.8 = 12.8 GiB\n  Card 1 free: 95.6 GiB ENTIRELY — second card released completely\n  TOTAL FREE ACROSS PAIR = 108.4 GiB vs the >=30 GB requirement -> 3.6x margin, requirement cleared decisively.\n  Concurrent workloads: MageZero 12.53 GB + gemma-4-12B QLoRA 11.6 GB = 24.1 GB, both fit on card 1 alone with ~71 GiB still spare.\n\nContext is NOT the binding constraint: even at the full 1M, KV = 4.00 GiB -> 86.3 GiB total, util 0.90, STILL single-card. The owner does not have to give up 1M context to get Tier A. (max_position_embeddings=262144 in config, so 256k is the drop-in default; 1M needs a rope/len override.)\n\nSWING VS INCUMBENT: DeepSeek V4 Flash = 79.7 GB/card x2 = 159.4 GB weights, 0.95 util is a FLOOR, ~2 GiB free across the pair today. Nemotron FP8 = ~108 GiB free. Net recovery ~107 GiB.\n\nNVFP4 would give ~40 GiB free on card 0 too, but see vllm_ready — do not use it on sm_120.",
        "vllm_ready": "ARCHITECTURE SUPPORT: CONFIRMED MERGED IN MAINLINE — survey never checked this and it is the single most load-bearing fact. vllm/model_executor/models/registry.py on main contains: \"NemotronHPuzzleForCausalLM\": (\"nemotron_h\", \"NemotronHForCausalLM\"). Latest vLLM is v0.26.0 (2026-07-27); card was tested on v0.20.0, so the arch has had 6 releases of soak time. Mamba n_groups=8 divides cleanly by TP=1/2/4, no TP constraint.\n\nFP8 ON sm_120: SAFE, and empirically proven on this exact host — the owner runs DeepSeek-V4-Flash-FP8 on these two cards today. That is stronger evidence than any doc.\n\nNVFP4 ON sm_120: SURVEY'S WARNING IS CORRECT AND I AM REINFORCING IT, but its evidence was stale in both directions.\n- Issue #31085 (native NVFP4 MoE for sm_120): I confirmed via GitHub API still state=open, created 2025-12-20. Survey right.\n- Issue #33416 (\"device capability family check missing SM120\"): survey missed it — opened 2026-01-30, CLOSED 2026-01-31. That is the is_device_capability_family(100) check the survey described as the root cause. It was fixed 6 months ago. The survey's stated mechanism is out of date.\n- PR #40082 \"Integrate flashinfer b12x MoE and FP4 GEMM kernels for SM120/121\": MERGED 2026-05-20. Survey said \"No linked PRs currently shown as fixing this.\" Wrong.\n- BUT the fix is not convincing: PR #40082's own measured gains were +1.8%/+6.0% and were benchmarked on SM121 (DGX Spark), not SM120, with a hard pin to nvidia-cutlass-dsl==4.4.2 (4.5.0 emits bad PTX). And an independent vLLM forum report on 4x RTX PRO 6000 (April 2026, Qwen3.5-397B-NVFP4) concludes \"the native CUTLASS NVFP4 MoE path is broken on SM120 due to TMA WS grouped GEMM initialization failures\" — best working config was 50.5 tok/s via Marlin W4A16 FALLBACK, which dequantizes FP4 to FP16 (you pay FP4's accuracy cost and get none of its speed); native attempts produced garbage at 6-7 tok/s.\n=> USE THE FP8 BUILD. The 27.5 GiB saved by NVFP4 is not worth it and you do not need it — FP8 already achieves Tier A.\n\nNEW RISK THE SURVEY MISSED: the card explicitly says (line 150) \"FP8 checkpoints target Hopper-class GPUs, while NVFP4 checkpoints target Blackwell-class GPUs,\" and the vLLM section (line 184) frames BF16/FP8 as the Hopper recipe. Tested hardware (lines 617-619) is 1xH100-80GB, 8xH100-80GB, 8xB200 — no RTX PRO 6000 anywhere. So on sm_120 you are off NVIDIA's tested matrix in BOTH directions: the Blackwell-targeted quant has broken kernels on your Blackwell variant, and the quant that works is the one validated on Hopper. Nobody tested the configuration you would actually run. Also note the card's \"single-H100\" 1M-concurrency claim cannot refer to the FP8 build (77.33 GiB will not fit an 80 GB H100) — do not treat that claim as validating the FP8 path.",
        "quality_vs_deepseek": "THE SURVEY'S QUALITY VERDICT IS INVALID — it compared against a DeepSeek mode this product does not use.\n\nSurvey said: \"82.0 MMLU-Pro is ~4.4 points below DeepSeek-V4-Flash's 86.4.\" I pulled DeepSeek-V4-Flash's own model card. That table has THREE Flash modes:\n| Benchmark | V4-Flash Non-Think | V4-Flash High | V4-Flash Max |\n| MMLU-Pro (EM) | 83.0 | 86.4 | 86.2 |\n| GPQA Diamond | 71.2 | 87.4 | 88.1 |\n86.4 is Flash HIGH — i.e. thinking mode. But this product deliberately disables extended reasoning on the real-time path (commit 200ecff, \"stop extended reasoning on the real-time path (5.5x faster)\"). The operative incumbent config is V4-Flash NON-THINK: MMLU-Pro 83.0, GPQA 71.2 — not 86.4/88.1.\n\nNaively re-anchored, that flips the result: Nemotron 82.0 vs 83.0 MMLU-Pro (-1.0), and 77.8 vs 71.2 GPQA (+6.6 AHEAD). BUT I will not assert that, because it commits the mirror-image error: Nemotron's published 82.0/77.8 are certainly thinking-ON numbers (card line 328: \"By default, enable_thinking is set to True\"; evals run via Nemo Evaluator SDK with no reasoning-off variant published).\n\nHONEST CONCLUSION: neither model card publishes the like-for-like number, so no valid comparison exists at this product's operating point. The gap could plausibly be anywhere from \"Nemotron ahead\" to \"Nemotron behind.\" There is also NO head-to-head anywhere — Nemotron's table (line 55) compares only its own BF16/FP8/NVFP4 siblings; different harnesses (Nemo Evaluator/Nemo Skills vs DeepSeek internal), different prompting. Cross-card deltas are indicative at best.\n\nAlso: MMLU-Pro and GPQA do not predict \"read a 6.3k-token structured MTG game state and pick the best legal action from a numbered menu.\" Verified secondary figures for the record — Nemotron FP8: MMLU-Pro 82.0, GPQA(no tools) 77.8, AIME25 89.4, Arena-Hard-V2 69.8, LiveCodeBench 80.5, RULER@1M 92.4. Compression really is near-free vs the 120B parent (82.4/78.6/89.7/68.6/81.1) — that part of the survey checks out.\n\nDECIDE THIS WITH tools/eval/, NOT BENCHMARK TABLES. The repo already has the right instrument: real captured prompts + a 4-dimension rubric. Run --backend openai-compatible|http://10.0.0.10:8002/v1|nemotron against the incumbent. The repo's own gate applies: legality < 4.5 disqualifies it for the autopilot path.",
        "reasoning": "VERDICT: TIER_A, adopt as the leading candidate — but on the FP8 build only, and gate the swap on a tools/eval/ run rather than on either model card.\n\nIndependently verified, four claims:\n1. DATE — CONFIRMED from primary source. 2026-07-06 per the model card's own Release Date field; HF createdAt 2026-06-24. 24 days old. No listicle-recency error here; the survey did this correctly, including catching the 2025-12 family-announcement trap. Added nuance: the checkpoint is new but inherits its parent's Dec-2025 data exposure.\n2. VRAM — SURVEY WRONG, VERDICT SURVIVES. Real weights are 77.33 GiB (FP8) and 49.83 GiB (NVFP4), not 75 GB / 44.5 GB; the survey read a parameter count as a byte count and dropped 4.72B unquantized BF16 params. Corrected, FP8 still fits ONE card at util ~0.87, frees the second card entirely, and leaves ~108 GiB across the pair against a >=30 GB requirement. The KV story is even better than advertised: only 8 attention layers with 2 KV heads means 0.50 GiB at 128k and just 4.00 GiB at the full 1M — versus 29.6 GB of KV for the incumbent. Context length is simply not a constraint for this model, so the \"don't rank down for 128k\" guidance never even binds.\n3. vLLM/sm_120 — the decisive new fact is one the survey never checked: NemotronHPuzzleForCausalLM is registered in vLLM main today. Its NVFP4 warning is right in conclusion but stale in mechanism (the family-check bug it cited was closed 2026-01-31; a b12x kernel PR merged 2026-05-20). Independent evidence from 4x RTX PRO 6000 still shows native NVFP4 MoE broken on SM120 with a silent Marlin W4A16 fallback, so FP8 is the correct build — and FP8 MoE on sm_120 is already proven on this exact host by the running DeepSeek deployment.\n4. QUALITY — UNRESOLVED, and the survey's \"-4.4 behind\" should be discarded. It benchmarked Nemotron against DeepSeek's thinking-mode score while this product runs DeepSeek non-thinking. Non-think Flash is 83.0/71.2. No like-for-like number is published for either side and no head-to-head exists.\n\nTOP UNMITIGATED RISK — LATENCY, not VRAM. This is a reasoning model with enable_thinking defaulting to True. This product has already shipped a 6.1s advice call that caused a documented failure, and has a commit specifically to kill extended reasoning on the real-time path. Dropping this in without forcing chat_template_kwargs={\"enable_thinking\": False} would reproduce that failure exactly. Encouragingly, the fundamentals point the right way: 9.3B active vs DeepSeek's 13B, TP=1 removes cross-GPU NCCL sync, and the checkpoint ships a trained MTP head for speculative decoding. The card also offers a middle setting ({\"enable_thinking\": True, \"low_effort\": True}) worth A/B-ing against fully-off for a menu-selection task. But that is a hypothesis to measure against the 0.9-1.1s budget, not a claim.\n\nLicence OpenMDW-1.1 confirmed on the card — permissive, survey correct. Card also notes \"This project is currently not accepting contributions.\"\n\nSUGGESTED NEXT STEP: stand it up on card 1 alongside the running DeepSeek (there is not room for both at DeepSeek's 0.95 floor — so this needs a maintenance window, or bring it up TP=1 after stopping DeepSeek), serve FP8 with --tensor-parallel-size 1 --max-model-len 131072 --gpu-memory-utilization 0.88 --kv-cache-dtype fp8, force thinking off, then run tools/eval/ head-to-head on captured prompts and check median latency plus the legality >= 4.5 gate before cutting the gateway over."
      },
      {
        "name": "Mistral Small 4 (Mistral-Small-4-119B-2603)",
        "date_confirmed": "2026-03-16 — CONFIRMED, survey agent was correct. 4.5 months old as of today; not stale.",
        "date_source": "PRIMARY: https://mistral.ai/news/mistral-small-4 — official Mistral announcement, publication date March 16, 2026. Corroborated three independent ways: (a) Simon Willison https://simonwillison.net/2026/Mar/16/mistral-small-4/ dated 2026-03-16, text \"Big new release from Mistral today\"; (b) Artificial Analysis model page states verbatim \"Mistral Small 4 (Reasoning) was released on March 16, 2026\"; (c) Mistral's own YYMM checkpoint suffix \"2603\" = March 2026. HF API metadata: createdAt=2026-01-23T13:14:14Z, lastModified=2026-07-15T12:23:29Z. The survey agent gets explicit credit here — it flagged that the HF createdAt (2026-01-23) is a ~2-month pre-launch staging artifact and must NOT be used as the release date. That is exactly the right methodology and it did not repeat the Llama-4-Scout error.",
        "stale": false,
        "fits_verdict": "TIER_B",
        "vram_math": "VERDICT: fits Tier B with MORE margin than the survey claimed — but both of the survey's numbers were wrong; it reached a right-ish answer through two compensating errors.\n\nWEIGHTS (FP8 E4M3, the shipped default checkpoint). Verified two independent ways:\n  (1) HF safetensors dtype census: F8_E4M3 = 117,879,865,344 params x 1 B = 117.88 GB; BF16 = 1,521,452,032 params x 2 B = 3.04 GB (vision tower + multi_modal_projector + lm_head + embeddings, per config modules_to_not_convert). TOTAL = 120.92 GB = 112.62 GiB.\n  (2) Actual HF file sum of the consolidated-*.safetensors shards = 120.93 GB. Exact match. This is a measured number, not a blog round number.\n  Survey said \"~119 GB\" — it conflated the param count with bytes and dropped the 1.52B BF16 modules. Minor undercount.\n  TRAP FLAGGED: the repo TOTAL is 241.89 GB, and Simon Willison writes \"242GB on Hugging Face\". That is DOUBLE-COUNTED — the repo ships both the mistral-format consolidated-* AND the HF-format model-* copies of the same FP8 weights. Sizing VRAM off repo size would be 2x wrong.\n  Per card at TP=2: 112.62 / 2 = 56.31 GiB.\n\nKV CACHE — SURVEY WAS WRONG BY ~13x. config.json text_config shows kv_lora_rank=256, q_lora_rank=1024, qk_nope_head_dim=64, qk_rope_head_dim=64, v_head_dim=128. This is DeepSeek-style MLA, NOT MHA. The survey applied the MHA formula (32 KV heads x 128 head_dim x 36 layers -> \"295 KB/token\") which does not apply. vLLM caches only the compressed latent: (kv_lora_rank 256 + qk_rope_head_dim 64) = 320 elems/layer/token x 36 layers = 11,520 elems/token.\n  BF16 KV = 22.50 KiB/token -> 128k seq = 2.81 GiB ; 8k seq = 180 MiB\n  FP8  KV = 11.25 KiB/token -> 128k seq = 1.41 GiB ; 8k seq =  90 MiB\n  Survey's \"1.2 GB per 8k sequence\" is really 180 MiB. Note: with MLA, vLLM does NOT shard KV across TP ranks (the latent is replicated), so per-card KV = full KV — already accounted for below.\n\nAGAINST 2 x 95.6 GiB = 191.2 GiB. Requirement >= 30 GB free = 27.94 GiB. Activations + CUDA graphs budgeted at 3 GiB/card.\n  util 0.80 (recipe default), max-model-len 131072, BF16 KV:\n    budget 76.5 GiB/card - 56.31 weights - 3 = 17.2 GiB KV pool (6.1 concurrent 128k seqs, or 98 concurrent 8k seqs)\n    FREE = 19.1 GiB/card = 38.2 GiB total  -> PASSES\n  util 0.70, max-model-len 131072, FP8 KV:\n    budget 66.9 GiB/card - 56.31 - 3 = 7.6 GiB KV pool (5.4x 128k, or 87 concurrent 8k seqs — vastly more than a single-user coach needs)\n    FREE = 28.7 GiB/card = 57.4 GiB total -> PASSES COMFORTABLY\n\nHEADROOM IS USABLE, NOT STRANDED — this matters and neither tier definition captures it. MageZero = 12.53 GB = 11.67 GiB; gemma-4-12B QLoRA = 11.6 GB = 10.80 GiB; sum = 22.47 GiB. At util 0.80 each job fits on its own card (19.1 GiB free per card). At util 0.70 a SINGLE card's 28.7 GiB free holds BOTH concurrently.\n\nLOAD-TIME SAFETY (directly addresses the 0.75 OOM incident): weights/card 56.31 GiB = 0.589 utilization, so the util FLOOR here is ~0.62-0.65. DeepSeek V4 Flash's 79.7 GB = 74.2 GiB/card = 0.776 floor, which is precisely why 0.75 OOM'd during weight load and took the service down. Mistral Small 4 gives ~18 GiB/card more load-time slack. 0.95 would no longer be a floor.\n\nTIER A CHECK (NVFP4, official mistralai/Mistral-Small-4-119B-2603-NVFP4, repo created 2026-03-03): file sum = 70.85 GB = 65.98 GiB. On ONE card: 95.6 - 65.98 - 3 = 26.6 GiB for KV = 19 concurrent 128k seqs at FP8 KV. Arithmetically Tier A, freeing the entire second card. BUT blocked on sm_120 kernels today — see vllm_ready. Do not plan on it.\nBonus: the eagle speculative-decoding head (mistralai/...-eagle) is only 0.38 GiB and is directly relevant to the latency budget.",
        "vllm_ready": "PROBABLE BUT UNVALIDATED ON sm_120 — this is the biggest execution risk, and the survey correctly declined to verify it (\"sm_120-specific FP8 MoE kernels not independently verified by me\"). It is worse than that phrasing implies.\n\nArchitecture is mainline: mistral3 / Mistral3ForConditionalGeneration, vLLM >= 0.20.0. But the official vLLM recipe page lists supported hardware as exactly \"2xB200 / 2xH200 / 2xMI300X (FP8 default) or 1xB200 with reduced context (NVFP4)\". sm_120 / RTX PRO 6000 is NOT on that list.\n\nMistral Small 4 is simultaneously MoE AND MLA — the two code paths with known sm_120 gaps:\n1. FP8 MoE: sm_120/sm_121 has no CUTLASS tensor/token-scaled FP8 grouped GEMM, so vLLM falls back to Triton MoE. This is the top latency risk. vLLM issue #44688 documents this exact failure mode on this exact card — Qwen3.6-35B-A3B-FP8 on RTX PRO 6000 \"loads and serves successfully but decode latency is much higher than expected and GPU utilization is very bursty\", with a missing tuned FP8 MoE config for the expert shape. Mistral Small 4 is E=128/topk=4, another shape unlikely to have a tuned config.\n2. MLA: the model card's recommended --attention-backend FLASH_ATTN_MLA is Hopper/sm_100-oriented; FlashAttention is reported unsupported on sm_120. TRITON_MLA is the working path — and tellingly, Mistral's own NVFP4 card already specifies --attention-backend TRITON_MLA.\n3. NVFP4 (the Tier A route) is effectively blocked: vLLM issue #31085 is OPEN since 2025-12-20 (affects 0.13.0) — sm_120 is not recognized in the FP4 backend-selection logic, so it silently falls back to Marlin weight-only with the explicit warning \"Your GPU does not have native support for FP4 computation... This may degrade performance for compute-heavy workloads.\" flashinfer issue #2577 reports NVFP4 mm_fp4 GEMM broken on sm_120 across all backends, and the native CUTLASS NVFP4 MoE path fails with TMA WS grouped-GEMM init errors.\n\nENCOURAGING PRECEDENT, with a caveat: the owner already runs DeepSeek V4 Flash — also MoE + MLA — on these exact cards at 0.9-1.1s. So the family demonstrably works on sm_120. But per the DeepSeek-V4-Flash HF discussion #28, that deployment required a `ds4-sm120-preview` branch plus VLLM_TRITON_MLA_SPARSE / *_ALLOW_CUDAGRAPH env vars, and went from 5 tok/s to 30-50 tok/s only once CUDA graphs were correctly enabled. sm_120 MoE+MLA is a hand-tuned path, not turnkey.\n\nBOTTOM LINE: expect it to RUN via Triton fallbacks, but do not assume it hits the 0.9-1.1s budget without benchmarking. Structurally it should be fast (6.5B active vs DeepSeek's 13B active, plus a 0.38 GiB eagle head for speculative decoding), but #44688 is proof that structural advantage can be erased by a missing sm_120 MoE kernel config. Benchmark before committing.",
        "quality_vs_deepseek": "SURVEY AGENT WAS WRONG HERE, AND THIS IS THE FINDING THAT SHOULD DRIVE THE DECISION. It stated \"no DeepSeek head-to-head published\" and rated confidence \"high\". A head-to-head does exist, on Artificial Analysis, and it is unfavorable:\n\n  Artificial Analysis Intelligence Index v4.1\n    Mistral Small 4 (Reasoning):              20\n    DeepSeek V4 Flash (Reasoning, High Effort): 37\n    DeepSeek V4 Flash (Reasoning, Max Effort):  40\n\nConsistent across two separate AA comparison pages (.../deepseek-v4-flash-vs-mistral-small-4 and .../deepseek-v4-flash-high-vs-mistral-small-4). That is roughly HALF the incumbent's score — a large regression, not parity.\n\nFAIR CAVEATS, stated so this is not over-read:\n- AAII v4.1 aggregates GDPval-AA v2, tau-3-Banking, Terminal-Bench v2.1, SciCode, Humanity's Last Exam, GPQA Diamond, CritPt, AA-Omniscience, AA-LCR. That is frontier research / agentic / coding difficulty — a weak proxy for \"read a ~6k-token structured game state, pick the best legal action from a numbered menu, explain in 1-2 sentences.\" The gap on THIS task is very likely smaller than 20-vs-40 suggests.\n- AA-LCR (long-context reasoning) is the most transferable sub-metric, and Mistral's own card claims 0.72 on it \"with just 1.6K characters, whereas Qwen models need 3.5-4x more output (5.8-6.1K)\". Terse, grounded output is directly valuable for a real-time TTS coaching product.\n- Mistral Small 4 ranks 7th of 62 in its class (class median 9). It is genuinely strong for its size — just not DeepSeek V4 Flash's equal.\n- The production path runs with reasoning DISABLED (commit 200ecff, \"stop extended reasoning on the real-time path\"). So neither model's reasoning-mode AAII is what actually ships. The non-reasoning gap is UNMEASURED — I am not asserting it is smaller, only that these numbers do not settle it.\n- Latency direction is favorable: AA medians give Mistral Small 4 TTFT 0.79s / 157 tok/s vs DeepSeek V4 Flash 1.47s / 122 tok/s. Cloud-provider medians, not sm_120 local, but 6.5B vs 13B active params is a real structural advantage.\n\nNo MTG-domain or game-state-reasoning benchmark exists for either model. Given the owner already has tools/eval/ (real captured coach prompts, LLM-as-judge on correctness/reasoning/conciseness/legality), the decisive test is cheap and already built. The published gap is large enough that I would NOT swap on paper — run the harness, and gate on legality >= 4.5 in particular, since picking from a numbered legal-action menu is exactly where a weaker model fails.",
        "reasoning": "TIER_B — confirmed, but with the survey's two key claims inverted: the VRAM case is BETTER than reported, and the quality case is substantially WORSE.\n\n1. DATE: verified 2026-03-16 from Mistral's own announcement, triple-corroborated. Survey correct, and it correctly rejected the HF createdAt of 2026-01-23 as pre-launch staging. Not stale.\n\n2. VRAM: the survey's tier was right but both its numbers were wrong, in opposite directions. Weights are 112.62 GiB (verified twice: dtype census and HF file sum 120.93 GB), not \"~119 GB\". More importantly the model is MLA (kv_lora_rank=256, qk_rope_head_dim=64), not MHA — so KV is 22.5 KiB/token, not the survey's 295 KB/token, a 13x overestimate. Consequence: the survey called the margin \"real but not generous\"; it is actually generous. Free VRAM is 38.2 GiB at the recipe's util 0.80 and 57.4 GiB at util 0.70, against a 27.94 GiB requirement. And the headroom is usable rather than stranded — at util 0.70 one card's 28.7 GiB free holds both MageZero (11.67 GiB) and the QLoRA job (10.80 GiB) together. Separately, the utilization floor drops from DeepSeek's 0.776 to 0.589, which directly removes the failure that took the service down at 0.75. I also flagged a live trap: the HF repo total is 241.89 GB (and one widely-read writeup repeats \"242GB\") because the repo ships the same FP8 weights twice in two formats — sizing off that would be 2x wrong.\n\n3. vLLM ON sm_120: the weakest link, and worse than the survey's hedge implied. This model is MoE AND MLA, the two paths with documented sm_120 gaps. FP8 MoE has no CUTLASS grouped GEMM on sm_120 and falls back to Triton — vLLM #44688 records exactly this on an RTX PRO 6000 producing \"much higher than expected\" decode latency for an FP8 MoE. The card's recommended FLASH_ATTN_MLA is unavailable on sm_120; TRITON_MLA is the real path. The NVFP4 checkpoint would arithmetically deliver TIER A (65.98 GiB on one card, freeing the second entirely), but that is blocked today by vLLM #31085 (open since 2025-12-20, silent Marlin weight-only fallback) and flashinfer #2577 (mm_fp4 broken on sm_120). So Tier A is not bankable. Encouragingly, DeepSeek V4 Flash is itself MoE+MLA and runs on these cards — but only via a preview branch and hand-set Triton MLA env vars, so this is a tuning project, not a drop-in.\n\n4. QUALITY: the decisive negative, and the survey missed it. It claimed no DeepSeek head-to-head exists; Artificial Analysis publishes one, and it shows Intelligence Index 20 for Mistral Small 4 versus 37-40 for DeepSeek V4 Flash, consistent across two pages. I discounted this appropriately — AAII is a frontier-difficulty aggregate (HLE, CritPt, Terminal-Bench) that poorly represents \"pick a legal action from a menu and justify it in two sentences,\" the model ranks 7/62 in its class, its AA-LCR long-context result with unusually terse output suits a TTS product, and production runs with reasoning off so neither model's reasoning score is what ships. But a ~2x published gap is far too large to wave through on the strength of caveats.\n\nNET RECOMMENDATION: Mistral Small 4 is a legitimate Tier B candidate on memory — it comfortably satisfies the >= 30 GB constraint, removes the OOM fragility, is Apache 2.0, and its 6.5B active params plus a 0.38 GiB eagle head point the right way on latency. It is NOT a safe swap on quality, and its sm_120 story requires the same hand-tuning the current DeepSeek deployment needed. Since the owner already has tools/eval/ with real captured coach prompts and a legality metric, the honest next step is to run that harness against Mistral Small 4 rather than decide from published aggregates — and to gate hard on legality, which is precisely where a model with half the intelligence index tends to break on numbered-menu selection."
      }
    ],
    "raw_count": 31
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Survey"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Recommend"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "survey:nemotron",
      "phaseIndex": 1,
      "phaseTitle": "Survey",
      "agentId": "a35d438154c1024d3",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442025027,
      "queuedAt": 1785442017907,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DIRECT ANSWER: **There is no Nemotron 4.** The latest gener…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785442551264,
      "tokens": 96946,
      "toolCalls": 48,
      "durationMs": 526237,
      "resultPreview": "{\"candidates\":[{\"name\":\"Nemotron 4 (DOES NOT EXIST AS OF 2026-07-30 — announced only, no weights)\",\"release_date\":\"not released\",\"release_date_source\":\"https://nvidianews.nvidia.com/news/nvidia-launches-nemotron-coalition-of-leading-global-ai-labs-to-advance-open-frontier-models\",\"params\":\"unannounced — no sizes, no architecture, no parameter counts published\",\"context\":\"unannounced\",\"vram_estimat…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "survey:recent-releases",
      "phaseIndex": 1,
      "phaseTitle": "Survey",
      "agentId": "a14e3257b0875b6c2",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442024103,
      "queuedAt": 1785442017907,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "METHODOLOGY. I did not rank anything from a listicle. Prima…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785442753160,
      "tokens": 132465,
      "toolCalls": 42,
      "durationMs": 729057,
      "resultPreview": "{\"candidates\":[{\"name\":\"Qwen3.6-27B (Alibaba/Qwen)\",\"release_date\":\"2026-04\",\"release_date_source\":\"https://github.com/QwenLM/Qwen3.6 — News entry verbatim: \\\"2026-04-22: Qwen3.6-27B is now availabe on Hugging Face Hub and ModelScope.\\\" Cross-checked against HF API createdAt=2026-04-21T07:50:43Z on https://huggingface.co/Qwen/Qwen3.6-27B\",\"params\":\"27.78B dense (no MoE). Verified from HF safetenso…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "survey:chinese-labs",
      "phaseIndex": 1,
      "phaseTitle": "Survey",
      "agentId": "a3b59134326f351fd",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442023114,
      "queuedAt": 1785442017907,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SCOPE RESULT: You asked me to cover Zhipu/Z.AI, Moonshot, M…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785442687429,
      "tokens": 111421,
      "toolCalls": 47,
      "durationMs": 664315,
      "resultPreview": "{\"candidates\":[{\"name\":\"Qwen3.6-27B (Alibaba/Qwen) — dense\",\"release_date\":\"2026-04\",\"release_date_source\":\"https://github.com/QwenLM/Qwen3.6 (official QwenLM repo release table: \\\"Qwen3.6-27B — Released 2026-04-22\\\"); corroborated by https://huggingface.co/Qwen/Qwen3.6-27B (repo last updated Apr 24, 2026)\",\"params\":\"27B total, DENSE — all 27B active per token. Not MoE.\",\"context\":\"262,144 native,…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "survey:right-sized",
      "phaseIndex": 1,
      "phaseTitle": "Survey",
      "agentId": "aed7964a4d4e0a24e",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442022358,
      "queuedAt": 1785442017907,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "THE SINGLE MOST IMPORTANT FINDING IS A DEPLOYMENT ONE, NOT …",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785442506586,
      "tokens": 90971,
      "toolCalls": 36,
      "durationMs": 484228,
      "resultPreview": "{\"candidates\":[{\"name\":\"NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B\",\"release_date\":\"2026-07\",\"release_date_source\":\"https://huggingface.co/nvidia/NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B-FP8\",\"params\":\"75.3B total / 9.3B active (Mamba2-Transformer hybrid LatentMoE + Multi-Token Prediction). Distilled/compressed from Nemotron-3-Super-120B-A12B via NVIDIA's Iterative Puzzle framework.\",\"context\":\"1M native …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:Nemotron 4 (DOES NOT EXI",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a0aca50eb3a90f361",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757232,
      "queuedAt": 1785442755440,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Nemotron 4 — CONFIRMED NON-EXISTENT as a 2026 model (name a…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785442908784,
      "tokens": 58969,
      "toolCalls": 12,
      "durationMs": 151552,
      "resultPreview": "{\"name\":\"Nemotron 4 — CONFIRMED NON-EXISTENT as a 2026 model (name also collides with a stale 2024 release)\",\"date_confirmed\":\"CONFIRMED with one correction. (a) The 2026 \\\"Nemotron 4 family\\\" has NO release date — it was announced only, on 2026-03-16 at GTC, and no weights exist as of 2026-07-30. (b) CORRECTION to the survey agent: \\\"Nemotron-4\\\" is a REUSED NAME. Weights literally called Nemotro…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:NVIDIA-Nemotron-3-Super-",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab895d389d3b81bc9",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442763496,
      "queuedAt": 1785442755440,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443220247,
      "tokens": 87533,
      "toolCalls": 25,
      "durationMs": 456749,
      "resultPreview": "{\"name\":\"NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4\",\"date_confirmed\":\"2026-03-10 — CONFIRMED, survey agent was right. HuggingFace API createdAt = 2026-03-10T18:33:04.000Z; NVIDIA official blog published 2026-03-11. Model is ~4.7 months old, comfortably past the 2025-12 staleness threshold. No listicle inference involved — this is the repo's own creation timestamp. (Repo lastModified 2026-05-01; link…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:Nemotron-3-Nano-Omni-30B",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ad45b3d65ec1a2a8f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757385,
      "queuedAt": 1785442755440,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443057927,
      "tokens": 72411,
      "toolCalls": 19,
      "durationMs": 300542,
      "resultPreview": "{\"name\":\"Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4\",\"date_confirmed\":\"2026-04-28 — CONFIRMED, and slightly more precise than the survey agent's \\\"2026-04\\\". Three primary sources agree: (a) the HF model card's own release table states \\\"Hugging Face 04/28/2026\\\" and \\\"Build.Nvidia.com 04/28/2026\\\"; (b) the official vLLM blog post announcing the model is dated 2026-04-28; (c) HF API lastModified…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:NVIDIA-Nemotron-3-Nano-3",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "acfa906119195ae36",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757269,
      "queuedAt": 1785442755440,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443048679,
      "tokens": 69287,
      "toolCalls": 15,
      "durationMs": 291410,
      "resultPreview": "{\"name\":\"NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4\",\"date_confirmed\":\"2025-12-20 (HF repo createdAt 2025-12-20T09:12:40.000Z); public announcement 2025-12-15. CONFIRMED — the survey agent's \\\"2025-12\\\" is correct, not a listicle inference. ~7.3 months old today (2026-07-30).\",\"date_source\":\"PRIMARY: https://huggingface.co/api/models/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4 → createdAt 2025-12-20T09:…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:Qwen3.6-27B (Alibaba/Qwe",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aecc7fb3ab44faf61",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757312,
      "queuedAt": 1785442755440,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Qwen3.6-27B (Alibaba/Qwen)",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443129797,
      "tokens": 76327,
      "toolCalls": 17,
      "durationMs": 372485,
      "resultPreview": "{\"name\":\"Qwen3.6-27B (Alibaba/Qwen)\",\"date_confirmed\":\"2026-04-21 (CONFIRMED, survey's \\\"2026-04\\\" is correct). HF API createdAt=2026-04-21T07:50:43Z for Qwen/Qwen3.6-27B; the FP8 repo Qwen/Qwen3.6-27B-FP8 createdAt=2026-04-21T07:51:33Z (53 seconds later — consistent with a coordinated official launch, not a backfill). Public announcement 2026-04-22. Age at 2026-07-30: ~3.3 months. Comfortably ins…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:Qwen3.6-35B-A3B (Alibaba",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a711237a046fbbdfb",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757321,
      "queuedAt": 1785442755441,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Qwen3.6-35B-A3B (Alibaba/Qwen)",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443155119,
      "tokens": 73125,
      "toolCalls": 18,
      "durationMs": 397798,
      "resultPreview": "{\"name\":\"Qwen3.6-35B-A3B (Alibaba/Qwen)\",\"date_confirmed\":\"2026-04-15 (HF repo creation) / 2026-04-16 (official announcement) — CONFIRMED, survey agent's \\\"2026-04\\\" is correct. 3.5 months old today (2026-07-30). Not stale.\",\"date_source\":\"Two independent primary sources agree. (1) HuggingFace API https://huggingface.co/api/models/Qwen/Qwen3.6-35B-A3B returns createdAt=2026-04-15T05:59:19.000Z, la…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:Gemma 4 31B (dense) and",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a4c9ddddbdf33b673",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757494,
      "queuedAt": 1785442755441,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Gemma 4 31B (dense) and Gemma 4 26B-A4B (MoE), Google DeepM…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443307987,
      "tokens": 95291,
      "toolCalls": 28,
      "durationMs": 550493,
      "resultPreview": "{\"name\":\"Gemma 4 31B (dense) and Gemma 4 26B-A4B (MoE), Google DeepMind\",\"date_confirmed\":\"2026-03-31 (models), 2026-04-02 (launch blog). CONFIRMED — survey agent was correct. ~4 months old, well inside the recency bar; not stale.\",\"date_source\":\"PRIMARY: ai.google.dev/gemma/docs/releases — \\\"March 31, 2026: Release of Gemma 4 in E2B, E4B, 31B and 26B A4B sizes\\\". Launch blog 2026-04-02. HuggingFa…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:DiffusionGemma 26B-A4B-i",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a8e27a2af94e3f3a3",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757720,
      "queuedAt": 1785442755441,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DiffusionGemma 26B-A4B-it (Google DeepMind)",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443039950,
      "tokens": 67120,
      "toolCalls": 14,
      "durationMs": 282229,
      "resultPreview": "{\"name\":\"DiffusionGemma 26B-A4B-it (Google DeepMind)\",\"date_confirmed\":\"2026-06-09 — CONFIRMED, survey agent was right. Triangulated across three independent sources: (1) HF API `createdAt` = 2026-06-09T12:40:12Z on the official `google` org repo `google/diffusiongemma-26B-A4B-it`; (2) NVIDIA's official blog post announcing it, published 2026-06-10; (3) ai.google.dev/gemma/docs/diffusiongemma/mode…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:NVIDIA Nemotron-Labs-3-P",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a80ba9edd80869f5b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442758151,
      "queuedAt": 1785442755441,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B (deploy the -FP8 buil…",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443172065,
      "tokens": 93015,
      "toolCalls": 18,
      "durationMs": 413913,
      "resultPreview": "{\"name\":\"NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B (deploy the -FP8 build, NOT -NVFP4)\",\"date_confirmed\":\"2026-07-06 — CONFIRMED, 24 days old. Survey's \\\"2026-07\\\" is correct.\",\"date_source\":\"PRIMARY: model card README.md line 98-99 verbatim: \\\"### Release Date: July 6, 2026 via Hugging Face\\\". Corroborated by HF API createdAt: FP8 repo = 2026-06-24T15:33:18Z, NVFP4 repo = 2026-06-24T15:31:37Z, both l…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:Mistral Small 4 (119B-26",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a9544c79e4622bb64",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785442757590,
      "queuedAt": 1785442755441,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Mistral Small 4 (Mistral-Small-4-119B-2603)",
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443149596,
      "tokens": 82076,
      "toolCalls": 24,
      "durationMs": 392006,
      "resultPreview": "{\"name\":\"Mistral Small 4 (Mistral-Small-4-119B-2603)\",\"date_confirmed\":\"2026-03-16 — CONFIRMED, survey agent was correct. 4.5 months old as of today; not stale.\",\"date_source\":\"PRIMARY: https://mistral.ai/news/mistral-small-4 — official Mistral announcement, publication date March 16, 2026. Corroborated three independent ways: (a) Simon Willison https://simonwillison.net/2026/Mar/16/mistral-small-…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "synthesis",
      "phaseIndex": 3,
      "phaseTitle": "Recommend",
      "agentId": "a3c9b286756dc3eb7",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785443314275,
      "queuedAt": 1785443309340,
      "attempt": 1,
      "promptPreview": "## The hardware and the constraint (identical for every agent)\n\nHost \"blackwell\". Two NVIDIA RTX PRO 6000 Blackwell Max-Q, 97,887 MiB each\n(~95.6 GiB), compute capability sm_120. Also an AMD Radeon AI PRO R9700\n(31.86 GiB, gfx1201, ROCm) which runs MageZero and is NOT part of this question.\n\nCURRENT STATE, measured today (2026-07-30), not quoted from docs:\n- DeepSeek V4 Flash serves via vLLM with …",
      "lastProgressAt": 1785443495410,
      "tokens": 131218,
      "toolCalls": 0,
      "durationMs": 181135,
      "resultPreview": "# Replacing DeepSeek V4 Flash on 2× RTX PRO 6000 Blackwell Max-Q (sm_120)\n**Date: 2026-07-30 · Constraint: ≥30 GB free after weights+KV, service up, ~0.9–1.1 s per advice call**\n\n---\n\n## 1. Is there a Nemotron 4? No.\n\n**There is no Nemotron 4 as of 2026-07-30.** NVIDIA announced the \"Nemotron Coalition\" on **2026-03-16** at GTC, and the press release contains exactly one sentence naming it: the co…"
    }
  ],
  "totalTokens": 1338175,
  "totalToolCalls": 363
}